---
phase: 10-advanced-analytics-features
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/modules/analytics/entities/analytics-ab-experiment.entity.ts
  - src/modules/analytics/services/funnel-analytics.service.ts
  - src/modules/analytics/services/ab-testing.service.ts
  - src/modules/analytics/analytics.controller.ts
  - src/modules/analytics/dto/funnel-query.dto.ts
  - src/modules/analytics/dto/funnel-response.dto.ts
  - src/modules/analytics/dto/ab-experiment.dto.ts
  - src/modules/analytics/analytics.module.ts
  - src/modules/intake/intake.service.ts
  - src/database/migrations/{timestamp}-CreateABExperiments.ts
  - test/analytics-funnel.e2e-spec.ts
autonomous: true
requirements: [DASH-04]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Funnel stage transitions (initiated → qualified → data_collected → exported → converted) são rastreadas via domain events"
    - "Drop-off rates calculados corretamente entre stages consecutivos"
    - "A/B test variant assignment usa consistent hashing garantindo mesmo user sempre recebe mesma variant"
    - "GET /api/analytics/funnel retorna conversion rates por variant com recomendações baseadas em performance"
  artifacts:
    - src/modules/analytics/entities/analytics-ab-experiment.entity.ts
    - src/modules/analytics/services/funnel-analytics.service.ts
    - src/modules/analytics/services/ab-testing.service.ts
    - test/analytics-funnel.e2e-spec.ts
  key_links:
    - "Intake service emite funnel.stage_entered events com stage + variantId computed via consistent hashing"
    - "FunnelAnalyticsService calcula drop-off rate via LAG window function per RESEARCH.md L361-367"
    - "AB experiment configuration stored in analytics_ab_experiments, variant assignment via crypto.createHash(userId + experimentId + salt)"
---

<objective>
Implementar intake funnel analytics com stage tracking, drop-off rate calculation, e A/B testing support. Estende Phase 1 Intake Bot com instrumentação de eventos de funil e adiciona análise estatística de conversão por variant.

Purpose: Entregar DASH-04 requirement (funnel tracking accuracy, A/B testing, conversion optimization) e habilitar data-driven iteration do intake flow baseado em métricas de drop-off por stage.

Output: AB experiment entity + service, funnel analytics service com cálculo de drop-off, domain events emitidos em intake.service.ts, três REST endpoints (/funnel, /experiments CRUD), suite E2E validando conversion rate tracking e variant consistency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-advanced-analytics-features/10-RESEARCH.md
@.planning/phases/01-bot-intake-e2e/01-01-SUMMARY.md
@.planning/phases/01-bot-intake-e2e/01-02-SUMMARY.md

# Reference existing event patterns:
@src/modules/analytics/listeners/analytics-event.listener.ts
@src/modules/analytics/entities/analytics-eentity.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A/B experiment entity + consistent hashing variant assignment service</name>
  <files>src/modules/analytics/entities/analytics-ab-experiment.entity.ts, src/modules/analytics/services/ab-testing.service.ts, src/modules/analytics/analytics.module.ts, src/database/migrations/{timestamp}-CreateABExperiments.ts, src/modules/analytics/services/ab-testing.service.spec.ts</files>
  <read_first>
    - .planning/phases/10-advanced-analytics-features/10-RESEARCH.md §10.2 "A/B Test Variant Assignment" (L283-332) — consistent hashing implementation, experiment configuration schema
    - src/modules/analytics/entities/analytics-event.entity.ts (schema reference)
  </read_first>
  <behavior>
    - assignVariant(userId, experimentId, variantCount) returns same variant string for same userId+experimentId combo across multiple calls
    - Hash distribution is uniform across variants (chi-square test on 1000 users shows <5% deviation from expected)
    - getActiveExperiment(experimentId) returns experiment config or null if not found/inactive
    - Variant assignment uses crypto.createHash('sha256') with salted input (userId + experimentId + AB_TEST_SALT)
  </behavior>
  <action>Create AnalyticsABExperiment entity (table analytics_ab_experiments per RESEARCH.md L307-318): id serial PK, experiment_id varchar(100) unique not null, name varchar(255) not null, description text nullable, variant_count integer not null default 2, variant_names jsonb (array of strings), start_date timestamp not null, end_date timestamp nullable, active boolean default true, created_at timestamp default now(). Create ABTestingService with assignVariant(userId: string, experimentId: string, variantCount: number): string method implementing consistent hashing per RESEARCH.md L290-303 (use crypto.createHash, salt from process.env.AB_TEST_SALT with fallback 'default-salt-change-me', modulo variant_count, return variant_${index}). Add getActiveExperiment(experimentId) querying repository for active experiment. Add @InjectRepository(AnalyticsABExperiment, 'data') to constructor. Register entity in analytics.module.ts TypeOrmModule.forFeature. Generate migration. Write ab-testing.service.spec.ts (RED first) with tests: (1) 'same user+experiment returns same variant' calling assignVariant 10 times, assert all equal; (2) 'variant distribution is uniform' assigning 1000 users, chi-square test on counts per variant <5% deviation; (3) 'getActiveExperiment returns config for active experiment' seeding experiment, assert returned. Add AB_TEST_SALT to .env.example with comment about security (change in production).</action>
  <verify>
    <automated>npx jest src/modules/analytics/services/ab-testing.service.spec.ts -x</automated>
  </verify>
  <done>
    - analytics_ab_experiments table exists with correct schema
    - ABTestingService.assignVariant() uses consistent hashing with crypto
    - Unit tests prove variant consistency and uniform distribution
    - AB_TEST_SALT env var documented in .env.example
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Funnel stage tracking via domain events + drop-off rate calculation service</name>
  <files>src/modules/analytics/services/funnel-analytics.service.ts, src/modules/intake/intake.service.ts, src/modules/analytics/analytics.module.ts, src/modules/analytics/services/funnel-analytics.service.spec.ts</files>
  <read_first>
    - .planning/phases/10-advanced-analytics-features/10-RESEARCH.md §10.2 "Funnel Stages Definition" (L245-276) — five stages + event schema
    - .planning/phases/10-advanced-analytics-features/10-RESEARCH.md "Drop-Off Rate Calculation" (L334-367) — SQL query with LAG window function
    - src/modules/intake/intake.service.ts (location to emit funnel events)
    - src/modules/analytics/listeners/analytics-event.listener.ts (event emission pattern)
  </read_first>
  <behavior>
    - computeFunnelStats(startDate, endDate, variantId?) returns stages array with [{stage, users, dropOffRate}]
    - Drop-off rate for stage N = 1 - (usersInStageN+1 / usersInStageN), null for first stage
    - Funnel events include variantId assigned via ABTestingService.assignVariant
    - getConversionRecommendations(variantStats) returns text recommendations when variant A has >10% better conversion than variant B
  </behavior>
  <action>Create FunnelAnalyticsService with computeFunnelStats(startDate: Date, endDate: Date, variantId?: string) method: query analytics_events where event_type='funnel.stage_entered', GROUP BY stage and variant_id, count distinct user_id per stage, compute drop-off rate using previous stage count (implement SQL per RESEARCH.md L345-367 using TypeORM QueryBuilder with window function LAG), return array of {stage, users, dropOffRate, previousStageUsers}. Add getConversionRecommendations(overallStats, byVariant) comparing conversion rates, returning string array of insights if delta >10%. Instrument src/modules/intake/intake.service.ts to emit funnel.stage_entered events at five transition points: (1) 'initiated' when first message received, (2) 'qualified' after qualification questions answered, (3) 'data_collected' when all required fields collected, (4) 'exported' when data exported to CRM, (5) emit funnel.completed when conversion happens. Each event includes {sessionId, userId, conversationId, stage, variantId: this.abTestingService.assignVariant(userId, 'intake-flow-v2', 2), timestamp}. Inject EventEmitter2 and ABTestingService into IntakeService constructor. Register FunnelAnalyticsService in analytics.module providers. Write funnel-analytics.service.spec.ts (RED first) seeding analytics_events with known funnel progression (100 initiated, 70 qualified, 50 data_collected, 40 exported, 20 converted), assert computeFunnelStats returns correct drop-off rates (30%, 28.5%, 20%, 50%).</action>
  <verify>
    <automated>npx jest src/modules/analytics/services/funnel-analytics.service.spec.ts -x && npx jest src/modules/intake/intake.service.spec.ts::funnelEvents -x</automated>
  </verify>
  <done>
    - FunnelAnalyticsService.computeFunnelStats() calculates drop-off rates correctly
    - IntakeService emits funnel.stage_entered at five transition points with variantId
    - Unit tests prove drop-off calculation accuracy with seeded data
    - Conversion recommendations generated when variant delta >10%
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Funnel analytics REST endpoints + A/B experiment CRUD + E2E validation</name>
  <files>src/modules/analytics/analytics.controller.ts, src/modules/analytics/dto/funnel-query.dto.ts, src/modules/analytics/dto/funnel-response.dto.ts, src/modules/analytics/dto/ab-experiment.dto.ts, test/analytics-funnel.e2e-spec.ts</files>
  <read_first>
    - .planning/phases/10-advanced-analytics-features/10-RESEARCH.md "Dashboard Integration" (L369-408) — API response schema
    - src/modules/analytics/analytics.controller.ts (existing endpoint patterns)
  </read_first>
  <behavior>
    - GET /api/analytics/funnel?startDate=X&endDate=Y&variantId=Z returns overallConversion + byVariant array + recommendations
    - POST /api/analytics/experiments creates new A/B experiment with validation (start_date < end_date, variant_count >= 2)
    - GET /api/analytics/experiments returns all experiments
    - PUT /api/analytics/experiments/:id updates experiment (only if not yet started or still active)
    - E2E test seeds funnel events across two variants, queries endpoint, asserts conversion rates and recommendations
  </behavior>
  <action>Create FunnelQueryDto (startDate optional Date, endDate optional Date, variantId optional string). Create FunnelResponseDto matching RESEARCH.md L374-407 (overallConversion: {initiated, qualified, data_collected, exported, converted, conversionRate}, byVariant: [{variantId, stages: [{stage, users, dropOffRate}], conversionRate}], recommendations: string[]). Create ABExperimentDto (experiment_id required, name required, description optional, variant_count default 2, variant_names string[], start_date required, end_date optional). Add GET /api/analytics/funnel in analytics.controller.ts with @Query() query: FunnelQueryDto, calling funnelAnalyticsService.computeFunnelStats and getConversionRecommendations, returning FunnelResponseDto. Add POST /experiments, GET /experiments, PUT /experiments/:id endpoints with @Body() validation via class-validator (start_date < end_date, variant_count >= 2). Write test/analytics-funnel.e2e-spec.ts with describe('Funnel Analytics E2E'): (1) 'tracks funnel stages and computes drop-off rates' seeding 100 initiated → 70 qualified → 50 data_collected → 40 exported → 20 converted events, calling GET /funnel, asserting overallConversion.conversionRate = 0.20; (2) 'A/B test variant assignment is consistent' assigning variant 100 times for same user, asserting all equal; (3) 'byVariant stats show correct conversion rates' seeding two variants with different conversion (control: 20%, treatment: 25%), asserting recommendations include "treatment has 25% higher conversion".</action>
  <verify>
    <automated>npx jest test/analytics-funnel.e2e-spec.ts -x</automated>
  </verify>
  <done>
    - GET /api/analytics/funnel returns overallConversion + byVariant + recommendations
    - POST/GET/PUT /api/analytics/experiments endpoints functional with validation
    - E2E tests prove funnel tracking accuracy and A/B variant consistency
    - Recommendations generated when variant performance differs >10%
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API /funnel | Conversion rate data reveals business performance metrics |
| IntakeService → EventEmitter | Funnel events contain user journey data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Information Disclosure | GET /funnel endpoint | medium | mitigate | Require operator role authentication (reuse auth guards) |
| T-10-06 | Tampering | A/B experiment salt predictability | high | mitigate | Enforce AB_TEST_SALT env var validation at startup (error if default value detected in production) |
| T-10-07 | Spoofing | POST /experiments unauthorized creation | high | mitigate | Require admin role for experiment CRUD (use existing role guards) |
| T-10-08 | Information Disclosure | Funnel events logging userId | low | accept | UserId is already tracked in analytics_events from Phase 6, no new PII exposure |
| T-10-09 | Denial of Service | Funnel query date range unbounded | medium | mitigate | Validate date range max 90 days in FunnelQueryDto using class-validator @MaxDateRange custom decorator |
</threat_model>

<verification>
## Phase 10 Plan 02 Verification

**Automated:**
```bash
npm run test:e2e -- --testPathPattern=analytics-funnel
```

**Manual gates:**
1. Visual inspection: Seed 100 intake sessions across control/treatment variants, verify funnel visualization shows different conversion rates
2. Consistency check: Call assignVariant() for same user 100 times, manually verify all return same variant
3. Drop-off calculation: Manually compute drop-off from stage counts, compare with API response

**Success criteria:**
- E2E test suite green (funnel tracking, A/B consistency, recommendations)
- Funnel endpoint returns correct conversion rates per variant
- A/B experiment CRUD endpoints functional with validation
- IntakeService emits funnel.stage_entered events at correct transition points
</verification>

<success_criteria>
## Measurable Completion

- ✅ AnalyticsABExperiment entity exists with correct schema
- ✅ ABTestingService.assignVariant() uses consistent hashing with uniform distribution
- ✅ FunnelAnalyticsService computes drop-off rates correctly via LAG window function
- ✅ IntakeService emits funnel.stage_entered events at five transition points
- ✅ GET /api/analytics/funnel returns overallConversion + byVariant + recommendations
- ✅ POST/GET/PUT /api/analytics/experiments CRUD endpoints functional
- ✅ E2E tests prove >80% accuracy in funnel tracking and variant consistency
- ✅ Conversion recommendations generated when variant delta >10%
- ✅ AB_TEST_SALT env var documented and validated
</success_criteria>

<output>
Create `.planning/phases/10-advanced-analytics-features/10-02-SUMMARY.md` when done
</output>
