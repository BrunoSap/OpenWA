---
phase: 02-validacao-e2e-texto-llm-rag
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - test/rag-e2e-cycle.e2e-spec.ts
  - test/rag-llm-judge.e2e-spec.ts
  - package.json
autonomous: true
requirements: [RAG-02, RAG-03, RAG-05, RAG-06]

estimate:
  tokens: 50000
  raw_tokens: 40000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Busca semântica fuzzy (similarity >= 0.8) retorna documentos relevantes mesmo com query parafraseada"
    - "LLM-as-judge evaluator valida que resposta usa contexto da KB (faithfulness check)"
    - "Query sem match na KB retorna resposta de fallback genérica (não alucina informação)"
    - "Teste valida que pgvector similarity score é >= 0.8 para matches válidos"
  artifacts:
    - test/rag-e2e-cycle.e2e-spec.ts (expandido com casos fuzzy e fallback)
    - test/rag-llm-judge.e2e-spec.ts (suite dedicada para validação LLM-as-judge)
    - package.json (dependências @langchain/core e langsmith adicionadas)
  key_links:
    - "E2E test → pgvector similarity query → assertion on similarity >= 0.8"
    - "LLM response → LLM-as-judge evaluator → faithfulness score"
    - "No-match query → fallback detection → assertion on generic response"

user_setup:
  - service: OpenAI
    why: "LLM-as-judge evaluator requires GPT-4o-mini for non-deterministic response validation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys (https://platform.openai.com/api-keys)"
    dashboard_config: []
---

<objective>
Expandir cobertura de testes RAG com casos fuzzy search, validação LLM-as-judge para respostas não-determinísticas, e fallback quando KB não tem match.

Purpose: Validar que o sistema RAG funciona além do happy path — queries parafraseadas encontram respostas relevantes, LLM usa contexto corretamente, e fallback previne alucinações.

Output: 3 novos test cases na suite RAG E2E + suite dedicada LLM-as-judge para validação semântica.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-01-PLAN.md
@test/rag-e2e-cycle.e2e-spec.ts
@test/fixtures/rag-test-knowledge.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências LangChain para LLM-as-judge</name>
  <files>package.json</files>
  <read_first>
    - package.json (seção devDependencies para adicionar pacotes de teste)
    - .planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md (versões recomendadas: @langchain/core latest, langsmith latest, zod 3.x)
  </read_first>
  <action>
Instalar dependências necessárias para validação LLM-as-judge em testes E2E.

**Pacotes a instalar:**
```bash
npm install --save-dev @langchain/core langsmith zod @faker-js/faker
```

**Justificativa (conforme RESEARCH.md):**
- @langchain/core: LLM-as-judge evaluators (correctness, faithfulness) para validação semântica de respostas LLM não-determinísticas
- langsmith: Evaluation framework para métricas RAG avançadas (precision@k, recall@k)
- zod: Schema validation para outputs estruturados do LLM-as-judge
- @faker-js/faker: Geração de dados de teste realistas (opcional, já listado no RESEARCH)

**Verificar instalação:**
```bash
npm list @langchain/core langsmith zod
```

Todos os pacotes foram auditados no RESEARCH.md Package Legitimacy Audit com verdict OK (approved).
  </action>
  <verify>
    <automated>
      npm list @langchain/core langsmith zod | grep -E "@langchain/core|langsmith|zod"
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - package.json contém @langchain/core, langsmith, zod em devDependencies
    - npm list @langchain/core mostra versão instalada (latest ~0.3.x)
    - npm list langsmith mostra versão instalada (latest ~0.2.x)
    - npm list zod mostra versão instalada (3.x)
    - npm run build completa sem erros (nenhuma breaking change introduzida)
  </acceptance_criteria>
  <done>
Dependências LangChain instaladas e prontas para uso em LLM-as-judge evaluators nos testes E2E.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expandir suite RAG E2E com casos fuzzy e fallback</name>
  <files>test/rag-e2e-cycle.e2e-spec.ts</files>
  <read_first>
    - test/rag-e2e-cycle.e2e-spec.ts (tracer existente do Plan 02-01)
    - test/fixtures/rag-test-knowledge.json (FAQs seedados para referência)
    - src/modules/knowledge/knowledge.service.ts (método de busca semântica e threshold de similarity)
  </read_first>
  <action>
Adicionar 3 novos test cases à suite RAG E2E existente: busca fuzzy, validação de similarity score, e fallback sem match.

**Test case 1 (RAG-05 - fuzzy search):**
```typescript
it('RAG-05: fuzzy semantic search returns relevant docs (similarity >= 0.8)', async () => {
  // Query parafraseada que deve encontrar o FAQ "Como dar entrada no INSS?"
  const fuzzyQuery = 'Quero fazer pedido de aposentadoria pelo INSS';
  
  const res = await request(app.getHttpServer())
    .post('/api/messages')
    .set('X-API-Key', apiKey)
    .send({ chatId: 'test-rag-fuzzy@c.us', text: fuzzyQuery })
    .expect(200);
  
  const reply = res.body.reply;
  
  // Deve mencionar "Meu INSS" ou "site" (do FAQ seedado)
  expect(reply.toLowerCase()).toMatch(/meu inss|site|agência/);
  
  // Log similarity score para verificação manual
  // (Nota: em prod isso viria do KnowledgeService.search metadata)
});
```

**Test case 2 (RAG-02 - validação de similarity):**
```typescript
it('RAG-02: pgvector similarity score >= 0.8 for valid matches', async () => {
  // Este teste valida a busca semântica diretamente via KnowledgeService
  const knowledgeService = app.get(KnowledgeService);
  
  const results = await knowledgeService.searchSimilarFAQs(
    'Como dar entrada no INSS?',
    { threshold: 0.8, limit: 5 }
  );
  
  expect(results.length).toBeGreaterThan(0);
  expect(results[0].similarity).toBeGreaterThanOrEqual(0.8);
  expect(results[0].question).toContain('INSS');
});
```

**Test case 3 (RAG-06 - fallback sem match):**
```typescript
it('RAG-06: no match in KB returns fallback response (no hallucination)', async () => {
  const unrelatedQuery = 'What is the meaning of life?';
  
  const res = await request(app.getHttpServer())
    .post('/api/messages')
    .set('X-API-Key', apiKey)
    .send({ chatId: 'test-rag-fallback@c.us', text: unrelatedQuery })
    .expect(200);
  
  const reply = res.body.reply;
  
  // Fallback deve reconhecer falta de contexto
  expect(reply.toLowerCase()).toMatch(
    /não sei|não tenho|base de conhecimento|não encontrei/
  );
  
  // NÃO deve alucinar informação específica de INSS
  expect(reply.toLowerCase()).not.toContain('inss');
});
```

Adicionar cleanup no afterEach para deletar leads de teste criados por esses novos chatIds.
  </action>
  <verify>
    <automated>npm run test:e2e -- --testPathPattern=rag-e2e-cycle</automated>
  </verify>
  <acceptance_criteria>
    - test/rag-e2e-cycle.e2e-spec.ts contém 4 test cases (1 do tracer + 3 novos)
    - Test case RAG-05 faz query parafraseada e asserta resposta contém fragmento da KB
    - Test case RAG-02 chama KnowledgeService.searchSimilarFAQs diretamente e valida similarity >= 0.8
    - Test case RAG-06 faz query não relacionada e asserta resposta de fallback (não alucina)
    - afterEach deleta leads de teste por chatIds: test-rag@c.us, test-rag-fuzzy@c.us, test-rag-fallback@c.us
    - npm run test:e2e -- --testPathPattern=rag-e2e-cycle exit code 0 (4/4 testes passam)
  </acceptance_criteria>
  <done>
Suite RAG E2E cobre busca exata, busca fuzzy, validação de similarity score, e fallback sem match.
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite LLM-as-judge para validação de faithfulness</name>
  <files>test/rag-llm-judge.e2e-spec.ts</files>
  <read_first>
    - .planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md (Pattern 1: E2E RAG Test with LLM-as-Judge, linhas 161-258)
    - test/rag-e2e-cycle.e2e-spec.ts (estrutura de setup: AppModule boot, API key, fixtures)
  </read_first>
  <action>
Criar suite dedicada para validação de faithfulness usando LLM-as-judge evaluator.

**Setup (igual ao rag-e2e-cycle):**
- Boot AppModule, seed TEST_FAQS de test/fixtures/rag-test-knowledge.json
- Criar API key ADMIN
- jest.setTimeout(60000)

**LLM-as-judge evaluator:**
```typescript
import { ChatOpenAI } from '@langchain/openai';
import { z } from 'zod';

const evaluatorLLM = new ChatOpenAI({
  model: 'gpt-4o-mini',
  temperature: 0,
  apiKey: process.env.OPENAI_API_KEY,
}).withStructuredOutput(
  z.object({
    explanation: z.string().describe('Explain your reasoning'),
    faithful: z.boolean().describe('True if answer uses KB context'),
  })
);
```

**Test case (RAG-03 - faithfulness validation):**
```typescript
it('RAG-03: LLM response uses retrieved context (validated by LLM-as-judge)', async () => {
  const question = 'Como dar entrada no INSS?';
  const expectedContext = 'Você pode dar entrada pelo site Meu INSS ou presencialmente';
  
  const res = await request(app.getHttpServer())
    .post('/api/messages')
    .set('X-API-Key', apiKey)
    .send({ chatId: 'test-llm-judge@c.us', text: question })
    .expect(200);
  
  const reply = res.body.reply;
  
  // Use LLM-as-judge to validate faithfulness
  const gradePrompt = `
    QUESTION: ${question}
    EXPECTED CONTEXT: ${expectedContext}
    LLM ANSWER: ${reply}
    
    Does the LLM answer contain information from the expected context?
    Be strict: the answer must reference specific details from the context.
  `;
  
  const grade = await evaluatorLLM.invoke([
    { role: 'system', content: 'You are a grading assistant for RAG systems.' },
    { role: 'user', content: gradePrompt }
  ]);
  
  expect(grade.faithful).toBe(true);
  console.log('LLM-as-judge explanation:', grade.explanation);
});
```

**Cleanup:**
- afterAll: deletar leads de teste
  </action>
  <verify>
    <automated>
      npm run test:e2e -- --testPathPattern=rag-llm-judge
    </automated>
  </verify>
  <acceptance_criteria>
    - test/rag-llm-judge.e2e-spec.ts existe e compila sem erros TypeScript
    - Suite possui describe block 'RAG LLM-as-judge validation (e2e)'
    - Test case RAG-03 usa ChatOpenAI com gpt-4o-mini e withStructuredOutput(zod schema)
    - Test case invoca LLM-as-judge com prompt de validação de faithfulness
    - Test case asserta grade.faithful === true
    - Suite requer OPENAI_API_KEY no ambiente (falha com mensagem clara se ausente)
    - npm run test:e2e -- --testPathPattern=rag-llm-judge exit code 0 quando OPENAI_API_KEY presente
  </acceptance_criteria>
  <done>
Suite LLM-as-judge valida que respostas LLM usam contexto da KB corretamente (faithfulness check).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → OpenAI API | LLM-as-judge sends prompts with test data to external API |
| Test suite → Groq API | RAG queries may trigger Groq LLM calls (fallback to OpenAI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information Disclosure | OPENAI_API_KEY in CI logs | medium | mitigate | Use GitHub Secrets for CI, never log API key value |
| T-02-05 | Denial of Service | Rate limit on OpenAI API | medium | accept | LLM-as-judge test runs sequentially, low request volume; document retry strategy |
| T-02-06 | Tampering | npm package install | high | mitigate | All packages verified in RESEARCH.md (langchain/core OK, langsmith OK, zod OK) |

</threat_model>

<verification>
**Phase gate:**
- npm run test:e2e -- --testPathPattern=rag-e2e-cycle exits 0 (4 testes)
- npm run test:e2e -- --testPathPattern=rag-llm-judge exits 0 (1 teste) quando OPENAI_API_KEY presente
- Test output mostra similarity scores >= 0.8 para matches válidos
- LLM-as-judge explanation logged mostra reasoning correto
</verification>

<success_criteria>
- ✅ Suite RAG E2E expandida com fuzzy search, similarity validation, fallback
- ✅ Suite LLM-as-judge criada e verde com validação de faithfulness
- ✅ Dependências LangChain instaladas (@langchain/core, langsmith, zod)
- ✅ User setup documented para OPENAI_API_KEY
- ✅ Todos os 5 test cases (4 rag-e2e-cycle + 1 rag-llm-judge) verdes
</success_criteria>

<output>
Create `.planning/phases/02-validacao-e2e-texto-llm-rag/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Test files:**
- test/rag-e2e-cycle.e2e-spec.ts (expandido: 4 test cases total)
- test/rag-llm-judge.e2e-spec.ts (nova suite: 1 test case)

**Dependencies:**
- @langchain/core (LLM-as-judge evaluators)
- langsmith (evaluation framework)
- zod (structured output validation)
- @faker-js/faker (test data generation)

**Test utilities:**
- ChatOpenAI evaluator with withStructuredOutput pattern
- LLM-as-judge prompt template for faithfulness validation

**Test coverage:**
- RAG-02: pgvector similarity score >= 0.8
- RAG-03: LLM uses KB context (LLM-as-judge validation)
- RAG-05: Fuzzy semantic search
- RAG-06: Fallback for no-match queries
