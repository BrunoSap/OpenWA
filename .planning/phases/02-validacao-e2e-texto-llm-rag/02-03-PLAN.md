---
phase: 02-validacao-e2e-texto-llm-rag
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - test/rag-performance.e2e-spec.ts
  - database/tests/validate_rag_retrieval.py
  - database/tests/fixtures/rag_golden_dataset.json
autonomous: true
requirements: [RAG-07, RAG-08]

estimate:
  tokens: 48000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Latência end-to-end do ciclo RAG medida e documentada (p50, p95, p99)"
    - "Latência p95 < 3000ms conforme requisito RAG-07"
    - "Precision@k >= 0.8 para golden dataset de queries anotadas"
    - "Métricas de qualidade RAG (precision, recall) calculadas e validadas"
  artifacts:
    - test/rag-performance.e2e-spec.ts
    - database/tests/validate_rag_retrieval.py
    - database/tests/fixtures/rag_golden_dataset.json
  key_links:
    - "Performance test → multiple RAG queries → latency percentile calculation"
    - "Python test → pgvector queries → precision@k/recall@k metrics"
    - "Golden dataset → ground truth annotations → retrieval quality validation"
---

<objective>
Criar testes de performance e métricas de qualidade RAG: latência end-to-end (p50/p95/p99) e precision@k/recall@k com golden dataset.

Purpose: Validar que o sistema RAG atende requisitos de performance (<3s latência) e qualidade (precision@k >= 0.8), provendo métricas objetivas para monitoramento.

Output: Suite de performance TypeScript + suite Python de métricas pgvector com golden dataset anotado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-01-PLAN.md
@test/rag-e2e-cycle.e2e-spec.ts
@test/fixtures/rag-test-knowledge.json
@database/tests/validate_performance_v2_simple.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite de performance para latência RAG</name>
  <files>test/rag-performance.e2e-spec.ts</files>
  <read_first>
    - test/rag-e2e-cycle.e2e-spec.ts (setup AppModule, API key, fixtures)
    - database/tests/validate_performance_v2_simple.py (padrão measure_query para cálculo de percentis)
    - .planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md (Pattern: pgvector Performance Measurement)
  </read_first>
  <action>
Criar suite E2E focada em medição de latência do ciclo RAG completo.

**Setup:**
- Boot AppModule, seed TEST_FAQS de test/fixtures/rag-test-knowledge.json
- Criar API key ADMIN
- jest.setTimeout(120000) (performance tests podem ser lentos)

**Performance test (RAG-07 - latência end-to-end):**
```typescript
describe('RAG Performance (e2e)', () => {
  it('RAG-07: end-to-end latency < 3000ms (p95)', async () => {
    const queries = [
      'Como dar entrada no INSS?',
      'Qual o prazo para recurso do INSS?',
      'Como solicitar auxílio-doença?',
      'Quero fazer pedido de aposentadoria',
      'Preciso entrar com recurso no INSS',
    ];
    
    const latencies: number[] = [];
    
    // Warm-up: primeira query para cache de conexões
    await request(app.getHttpServer())
      .post('/api/messages')
      .set('X-API-Key', apiKey)
      .send({ chatId: 'warmup@c.us', text: queries[0] });
    
    // Measure: executar cada query N vezes
    const iterations = 10;
    for (const query of queries) {
      for (let i = 0; i < iterations; i++) {
        const start = Date.now();
        await request(app.getHttpServer())
          .post('/api/messages')
          .set('X-API-Key', apiKey)
          .send({ 
            chatId: `perf-test-${i}@c.us`, 
            text: query 
          });
        const latency = Date.now() - start;
        latencies.push(latency);
      }
    }
    
    // Calculate percentiles
    latencies.sort((a, b) => a - b);
    const p50 = latencies[Math.floor(latencies.length * 0.50)];
    const p95 = latencies[Math.floor(latencies.length * 0.95)];
    const p99 = latencies[Math.floor(latencies.length * 0.99)];
    const avg = latencies.reduce((a, b) => a + b, 0) / latencies.length;
    
    console.log('RAG Latency Metrics:');
    console.log(`  Samples: ${latencies.length}`);
    console.log(`  Avg: ${avg.toFixed(2)}ms`);
    console.log(`  p50: ${p50}ms`);
    console.log(`  p95: ${p95}ms`);
    console.log(`  p99: ${p99}ms`);
    
    // Assert p95 < 3000ms (requirement)
    expect(p95).toBeLessThan(3000);
  });
});
```

**Cleanup:**
- afterAll: deletar leads de teste criados pelas queries de performance
  </action>
  <verify>
    <automated>npm run test:e2e -- --testPathPattern=rag-performance</automated>
  </verify>
  <acceptance_criteria>
    - test/rag-performance.e2e-spec.ts existe e compila sem erros TypeScript
    - Suite possui describe block 'RAG Performance (e2e)'
    - Test case RAG-07 executa warm-up query seguida de 5 queries × 10 iterações cada (50 medições)
    - Test case calcula p50, p95, p99, avg e loga valores no console
    - Test case asserta p95 < 3000ms
    - afterAll deleta leads de teste por chatId prefix 'perf-test-'
    - npm run test:e2e -- --testPathPattern=rag-performance exit code 0
  </acceptance_criteria>
  <done>
Suite de performance mede latência RAG end-to-end com percentis p50/p95/p99 e valida requisito < 3s.
  </done>
</task>

<task type="auto">
  <name>Task 2: Golden dataset para métricas de qualidade RAG</name>
  <files>database/tests/fixtures/rag_golden_dataset.json</files>
  <read_first>
    - test/fixtures/rag-test-knowledge.json (FAQs de teste existentes)
    - .planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md (Code Examples: Precision@K Calculation)
  </read_first>
  <action>
Criar golden dataset com queries anotadas e documentos relevantes para cálculo de precision@k/recall@k.

**Estrutura JSON:**
```json
[
  {
    "query": "Como dar entrada no INSS?",
    "query_embedding": [/* 1536 floats - embedding da query */],
    "relevant_doc_ids": [1],
    "notes": "Exact match - FAQ ID 1 from test fixtures"
  },
  {
    "query": "Quero fazer pedido de aposentadoria pelo INSS",
    "query_embedding": [/* 1536 floats */],
    "relevant_doc_ids": [1],
    "notes": "Paraphrased query - should retrieve FAQ ID 1"
  },
  {
    "query": "Qual o prazo para recurso?",
    "query_embedding": [/* 1536 floats */],
    "relevant_doc_ids": [2],
    "notes": "Partial match - FAQ ID 2 about INSS appeal deadline"
  },
  {
    "query": "Como solicitar auxílio-doença?",
    "query_embedding": [/* 1536 floats */],
    "relevant_doc_ids": [3],
    "notes": "Exact match - FAQ ID 3"
  },
  {
    "query": "Preciso de ajuda com benefício por doença",
    "query_embedding": [/* 1536 floats */],
    "relevant_doc_ids": [3],
    "notes": "Semantic match - should retrieve FAQ ID 3"
  }
]
```

**Golden dataset construction:**
- 5 queries anotadas manualmente com relevant_doc_ids
- Cada query tem embedding pré-computado (usar OpenAI text-embedding-3-small ou placeholder)
- relevant_doc_ids referencia os IDs dos FAQs de test/fixtures/rag-test-knowledge.json seedados na tabela

**Assumptions:**
- Assumir que os FAQs seedados terão IDs sequenciais 1, 2, 3 na tabela knowledge.faq (ajustar se necessário)
- Embeddings podem ser placeholders inicialmente (substituir por reais se precisão for crítica)
  </action>
  <verify>
    <automated>
      cat database/tests/fixtures/rag_golden_dataset.json | jq '.[0].query_embedding | length' | grep 1536
      cat database/tests/fixtures/rag_golden_dataset.json | jq 'length' | grep 5
    </automated>
  </verify>
  <acceptance_criteria>
    - database/tests/fixtures/rag_golden_dataset.json existe e é JSON válido
    - Arquivo contém array de 5 objetos
    - Cada objeto tem campos: query, query_embedding (1536 floats), relevant_doc_ids (array de ints), notes (string)
    - relevant_doc_ids referenciam IDs de FAQs de test/fixtures/rag-test-knowledge.json
    - jq validations passam: length=5, query_embedding length=1536
  </acceptance_criteria>
  <done>
Golden dataset anotado com 5 queries e documentos relevantes pronto para cálculo de precision@k/recall@k.
  </done>
</task>

<task type="auto">
  <name>Task 3: Script Python para métricas de qualidade RAG</name>
  <files>database/tests/validate_rag_retrieval.py</files>
  <read_first>
    - database/tests/validate_performance_v2_simple.py (padrão de conexão PostgreSQL, measure_query)
    - database/tests/fixtures/rag_golden_dataset.json (estrutura do golden dataset)
    - .planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md (Code Examples: Precision@K Calculation, Pattern 2: pgvector Retrieval Quality Test)
  </read_first>
  <action>
Criar script Python que calcula precision@k e recall@k usando o golden dataset.

**Script structure:**
```python
#!/usr/bin/env python3
"""
RAG Retrieval Quality Validation (RAG-08)

Measures precision@k and recall@k for pgvector semantic search using
a golden dataset of annotated queries with known relevant documents.

Usage:
  python database/tests/validate_rag_retrieval.py

Requirements:
  - PostgreSQL with pgvector extension
  - knowledge.faq table seeded with test FAQs
  - Golden dataset: database/tests/fixtures/rag_golden_dataset.json
"""

import json
import psycopg2
import numpy as np
from typing import List, Tuple

def load_golden_dataset(path: str) -> List[dict]:
    """Load annotated queries with known relevant doc IDs"""
    with open(path, 'r') as f:
        return json.load(f)

def calculate_precision_at_k(
    retrieved_ids: List[int], 
    relevant_ids: List[int], 
    k: int
) -> float:
    """Precision@k: fraction of top-k retrieved docs that are relevant"""
    retrieved_top_k = retrieved_ids[:k]
    relevant_set = set(relevant_ids)
    relevant_retrieved = len([id for id in retrieved_top_k if id in relevant_set])
    return relevant_retrieved / k if k > 0 else 0.0

def calculate_recall_at_k(
    retrieved_ids: List[int], 
    relevant_ids: List[int], 
    k: int
) -> float:
    """Recall@k: fraction of relevant docs that appear in top-k results"""
    retrieved_top_k_set = set(retrieved_ids[:k])
    relevant_set = set(relevant_ids)
    relevant_retrieved = len(retrieved_top_k_set & relevant_set)
    return relevant_retrieved / len(relevant_set) if relevant_set else 0.0

def test_rag_precision_at_k():
    """RAG-08: Measure precision@k and recall@k for semantic search"""
    conn = psycopg2.connect(
        dbname='openwa',
        user='openwa',
        host='localhost',
        port=5432
    )
    cursor = conn.cursor()
    
    golden_dataset = load_golden_dataset(
        'database/tests/fixtures/rag_golden_dataset.json'
    )
    
    precision_scores = []
    recall_scores = []
    
    for item in golden_dataset:
        query_embedding = item['query_embedding']
        relevant_ids = item['relevant_doc_ids']
        
        # Execute pgvector similarity search (RAG-02)
        cursor.execute("""
            SELECT id, 1 - (embedding <=> %s::vector) AS similarity
            FROM knowledge.faq
            WHERE category = 'test_rag_cycle' AND embedding IS NOT NULL
            ORDER BY embedding <=> %s::vector
            LIMIT 5
        """, (query_embedding, query_embedding))
        
        retrieved_ids = [row[0] for row in cursor.fetchall()]
        
        # Calculate precision@5 and recall@5
        precision = calculate_precision_at_k(retrieved_ids, relevant_ids, 5)
        recall = calculate_recall_at_k(retrieved_ids, relevant_ids, 5)
        
        precision_scores.append(precision)
        recall_scores.append(recall)
        
        print(f"Query: {item['query'][:50]}...")
        print(f"  Retrieved IDs: {retrieved_ids}")
        print(f"  Relevant IDs: {relevant_ids}")
        print(f"  Precision@5: {precision:.2f}")
        print(f"  Recall@5: {recall:.2f}")
    
    avg_precision = np.mean(precision_scores)
    avg_recall = np.mean(recall_scores)
    
    print(f"\n=== RAG Retrieval Quality Metrics ===")
    print(f"Samples: {len(golden_dataset)}")
    print(f"Average Precision@5: {avg_precision:.2f}")
    print(f"Average Recall@5: {avg_recall:.2f}")
    
    # RAG-08: Assert precision@5 >= 0.8
    assert avg_precision >= 0.8, f"Precision@5 too low: {avg_precision:.2f}"
    
    print(f"\n✅ RAG-08 PASSED: Precision@5 >= 0.8")
    
    cursor.close()
    conn.close()

if __name__ == '__main__':
    test_rag_precision_at_k()
```

Requer dependências já presentes: psycopg2, numpy (verificados em validate_performance_v2_simple.py).
  </action>
  <verify>
    <automated>
      python3 database/tests/validate_rag_retrieval.py 2>/dev/null || echo "Run after seeding test FAQs in PostgreSQL"
    </automated>
  </verify>
  <acceptance_criteria>
    - database/tests/validate_rag_retrieval.py existe e tem shebang #!/usr/bin/env python3
    - Script possui docstring documentando propósito e uso
    - Script define funções calculate_precision_at_k e calculate_recall_at_k
    - Função test_rag_precision_at_k carrega golden dataset e executa queries pgvector
    - Script calcula precision@5 e recall@5 para cada query do golden dataset
    - Script printa métricas individuais e médias no console
    - Script asserta avg_precision >= 0.8 (RAG-08)
    - Script possui if __name__ == '__main__' para execução standalone
  </acceptance_criteria>
  <done>
Script Python calcula e valida métricas de qualidade RAG (precision@k, recall@k) usando golden dataset.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Performance test → PostgreSQL | Multiple rapid queries may stress DB connection pool |
| Python script → PostgreSQL | Direct SQL queries with embedding vectors (trusted test data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial of Service | Performance test rapid queries | medium | mitigate | Warm-up query prevents cold start; connection pool sized for burst |
| T-02-08 | Information Disclosure | Latency metrics in logs | low | accept | Metrics are operational data, not sensitive; help with optimization |
| T-02-09 | Tampering | Python psycopg2 dependency | medium | accept | psycopg2 verified in RESEARCH.md as standard PostgreSQL driver |

</threat_model>

<verification>
**Phase gate:**
- npm run test:e2e -- --testPathPattern=rag-performance exits 0
- Test output shows p95 latency < 3000ms
- python3 database/tests/validate_rag_retrieval.py exits 0 (precision@5 >= 0.8)
- Both test suites log detailed metrics for monitoring baseline
</verification>

<success_criteria>
- ✅ Suite de performance mede latência RAG end-to-end (p50/p95/p99)
- ✅ Latência p95 < 3000ms validada
- ✅ Golden dataset anotado com 5 queries criado
- ✅ Script Python calcula precision@k e recall@k
- ✅ Precision@5 >= 0.8 validada com golden dataset
</success_criteria>

<output>
Create `.planning/phases/02-validacao-e2e-texto-llm-rag/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Test files:**
- test/rag-performance.e2e-spec.ts (suite de performance com latência p50/p95/p99)

**Python scripts:**
- database/tests/validate_rag_retrieval.py (métricas precision@k/recall@k)

**Fixtures:**
- database/tests/fixtures/rag_golden_dataset.json (5 queries anotadas com relevant_doc_ids)

**Functions:**
- calculate_precision_at_k(retrieved_ids, relevant_ids, k) → float
- calculate_recall_at_k(retrieved_ids, relevant_ids, k) → float

**Metrics produced:**
- RAG latency: avg, p50, p95, p99 (milliseconds)
- Precision@5: average across golden dataset
- Recall@5: average across golden dataset

**Test coverage:**
- RAG-07: End-to-end latency < 3000ms (p95)
- RAG-08: Precision@k measurement and validation (>= 0.8)
