---
phase: 02-validacao-e2e-texto-llm-rag
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - test/rag-e2e-cycle.e2e-spec.ts
  - test/fixtures/rag-test-knowledge.json
  - database/tests/fixtures/seed_test_faq.sql
autonomous: true
requirements: [RAG-01, RAG-02, RAG-03, RAG-04]

estimate:
  tokens: 45000
  raw_tokens: 35000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Mensagem WhatsApp simulada com pergunta da KB retorna resposta contextualizada do LLM"
    - "pgvector busca semântica retorna documentos relevantes (similarity >= 0.8)"
    - "LLM usa contexto da KB na composição da resposta (não resposta genérica)"
    - "Teste E2E verde percorre: HTTP POST → service → pgvector → LLM → resposta"
  artifacts:
    - test/rag-e2e-cycle.e2e-spec.ts
    - test/fixtures/rag-test-knowledge.json
    - database/tests/fixtures/seed_test_faq.sql
  key_links:
    - "E2E test → MessageService → KnowledgeService → pgvector query"
    - "KnowledgeService → LLM provider → response assembly"
    - "Test fixtures → knowledge.faq table → semantic search results"
---

<objective>
Criar tracer E2E que valida o ciclo RAG completo em um único caminho: mensagem WhatsApp → busca semântica pgvector → LLM contextualizado → resposta.

Purpose: Provar que a arquitetura RAG funciona end-to-end com dados reais de teste antes de expandir cobertura (casos fuzzy, fallback, métricas).

Output: Suite E2E verde com 1 caso de busca exata (similarity=1.0) provando a fatia vertical RAG.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-RESEARCH.md
@.planning/phases/01-bot-de-intake-e2e/01-01-SUMMARY.md
@.planning/phases/01-bot-de-intake-e2e/01-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer E2E - RAG happy path (busca exata)</name>
  <files>test/rag-e2e-cycle.e2e-spec.ts</files>
  <read_first>
    - test/intake-e2e-cycle.e2e-spec.ts (padrão E2E do projeto: AppModule boot, applyGlobalValidation, supertest, API key, jest.setTimeout(60000))
    - src/modules/knowledge/knowledge.service.ts (métodos de busca semântica existentes)
    - database/CURRENT_SCHEMA_EXPORT.sql (schema knowledge.faq com coluna embedding vector(1536))
  </read_first>
  <action>
Criar suite E2E que valida o ciclo RAG completo com um caso de busca exata.

**Setup:**
- Boot AppModule completo (Test.createTestingModule + applyGlobalValidation)
- jest.setTimeout(60000) (boot pode exceder 5s default)
- Criar API key ADMIN via AuthService.createApiKey
- beforeAll: seed 3 FAQs de teste na tabela knowledge.faq com embeddings pré-computados e category='test_rag_cycle' para isolamento
- afterAll: deletar FAQs de teste por category='test_rag_cycle', fechar app

**Test case único (RAG-01, RAG-04 busca exata):**
- POST /api/messages com chatId='test-rag@c.us' e text='Como dar entrada no INSS?' (pergunta que deve ter match exato no FAQ seedado)
- Medir latência (Date.now antes/depois do request)
- Assert: status 200
- Assert: res.body.reply contém fragmento-chave da resposta esperada (ex: 'Meu INSS' ou 'site' ou 'presencialmente')
- Assert: latência < 3000ms (RAG-07 início)
- Log embeddings retrieval no console para verificação manual durante desenvolvimento

**Seed fixture inline no beforeAll:**
```typescript
const TEST_FAQS = [
  {
    question: 'Como dar entrada no INSS?',
    answer: 'Você pode dar entrada pelo site Meu INSS ou presencialmente em uma agência.',
    category: 'test_rag_cycle',
    embedding: [/* 1536 floats - embedding pré-computado */],
  },
  // + 2 FAQs irrelevantes para volume
];
```

**Não implementar neste tracer:**
- Múltiplos casos de teste (fuzzy, fallback) → Wave 2
- Validação de similarity score explícita → Plan 02
- LLM-as-judge evaluator → Plan 02
- Métricas precision@k → Plan 03
  </action>
  <verify>
    <automated>npm run test:e2e -- --testPathPattern=rag-e2e-cycle</automated>
  </verify>
  <acceptance_criteria>
    - test/rag-e2e-cycle.e2e-spec.ts existe e compila sem erros TypeScript
    - Suite possui describe block 'RAG E2E (full cycle)' com 1 test case 'RAG-01/04: exact match query returns contextual LLM response'
    - beforeAll seeds 3 FAQs com category='test_rag_cycle' na tabela knowledge.faq
    - afterAll deleta FAQs por category='test_rag_cycle'
    - Test case faz POST /api/messages e asserta status 200 + reply contém fragmento da resposta esperada
    - Test case mede latência e asserta < 3000ms
    - npm run test:e2e -- --testPathPattern=rag-e2e-cycle exit code 0 (todos testes passam)
  </acceptance_criteria>
  <done>
Tracer E2E verde provando o ciclo RAG: mensagem → pgvector busca → LLM resposta contextualizada → latência < 3s.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fixture de embeddings pré-computados</name>
  <files>test/fixtures/rag-test-knowledge.json</files>
  <read_first>
    - test/rag-e2e-cycle.e2e-spec.ts (TEST_FAQS inline do Task 1 para extrair estrutura)
  </read_first>
  <action>
Criar arquivo de fixture JSON com os 3 FAQs de teste e embeddings pré-computados.

**Estrutura do JSON:**
```json
[
  {
    "question": "Como dar entrada no INSS?",
    "answer": "Você pode dar entrada pelo site Meu INSS ou presencialmente em uma agência.",
    "category": "test_rag_cycle",
    "embedding": [/* array de 1536 floats */],
    "tags": ["previdência", "INSS"]
  },
  {
    "question": "Qual o prazo para recurso do INSS?",
    "answer": "O prazo é de 30 dias corridos a partir da ciência da decisão.",
    "category": "test_rag_cycle",
    "embedding": [/* array de 1536 floats */],
    "tags": ["INSS", "recurso"]
  },
  {
    "question": "Como solicitar auxílio-doença?",
    "answer": "Solicite pelo aplicativo Meu INSS ou ligue para 135.",
    "category": "test_rag_cycle",
    "embedding": [/* array de 1536 floats */],
    "tags": ["INSS", "auxílio"]
  }
]
```

**Embeddings:**
- Gerar embeddings reais usando OpenAI text-embedding-3-small ou deixar placeholder [0.1, 0.2, ...] com 1536 dimensões para o primeiro FAQ
- Os outros 2 FAQs podem ter embeddings genéricos distintos (não precisam ser semanticamente corretos, apenas volume para teste)

Atualizar Task 1 para carregar do arquivo fixture em vez de inline no beforeAll.
  </action>
  <verify>
    <automated>
      cat test/fixtures/rag-test-knowledge.json | jq '.[0].embedding | length' | grep 1536
      npm run test:e2e -- --testPathPattern=rag-e2e-cycle
    </automated>
  </verify>
  <acceptance_criteria>
    - test/fixtures/rag-test-knowledge.json existe e é JSON válido
    - Arquivo contém array de 3 objetos com campos: question, answer, category, embedding, tags
    - Cada embedding é array de 1536 números float
    - category='test_rag_cycle' em todos os 3 objetos
    - test/rag-e2e-cycle.e2e-spec.ts foi atualizado para usar fs.readFileSync + JSON.parse do fixture
    - npm run test:e2e -- --testPathPattern=rag-e2e-cycle continua verde (exit code 0)
  </acceptance_criteria>
  <done>
Fixture JSON com 3 FAQs de teste + embeddings 1536-dim disponível para reuso em outras suites de teste RAG.
  </done>
</task>

<task type="auto">
  <name>Task 3: SQL seed script para fixtures de teste</name>
  <files>database/tests/fixtures/seed_test_faq.sql</files>
  <read_first>
    - test/fixtures/rag-test-knowledge.json (estrutura dos FAQs de teste)
    - database/CURRENT_SCHEMA_EXPORT.sql (schema knowledge.faq: colunas id, question, answer, category, tags, embedding, created_at)
  </read_first>
  <action>
Criar script SQL para seed manual dos FAQs de teste na tabela knowledge.faq.

**Script SQL:**
```sql
-- Seed test FAQs for RAG E2E testing
-- Category: test_rag_cycle (isolated, cleaned by afterAll)

DELETE FROM knowledge.faq WHERE category = 'test_rag_cycle';

INSERT INTO knowledge.faq (question, answer, category, tags, embedding, created_at)
VALUES
  ('Como dar entrada no INSS?',
   'Você pode dar entrada pelo site Meu INSS ou presencialmente em uma agência.',
   'test_rag_cycle',
   ARRAY['previdência', 'INSS'],
   '[0.1, 0.2, ...]'::vector(1536),  -- embedding pré-computado do fixture
   NOW()),
  ('Qual o prazo para recurso do INSS?',
   'O prazo é de 30 dias corridos a partir da ciência da decisão.',
   'test_rag_cycle',
   ARRAY['INSS', 'recurso'],
   '[0.15, 0.25, ...]'::vector(1536),
   NOW()),
  ('Como solicitar auxílio-doença?',
   'Solicite pelo aplicativo Meu INSS ou ligue para 135.',
   'test_rag_cycle',
   ARRAY['INSS', 'auxílio'],
   '[0.12, 0.22, ...]'::vector(1536),
   NOW());
```

Copiar embeddings reais do test/fixtures/rag-test-knowledge.json para os valores do INSERT.

**Uso:**
- Seed manual: `psql -U openwa -d openwa -f database/tests/fixtures/seed_test_faq.sql`
- Cleanup: `DELETE FROM knowledge.faq WHERE category = 'test_rag_cycle';`
  </action>
  <verify>
    <automated>
      psql -U openwa -d openwa -c "SELECT COUNT(*) FROM knowledge.faq WHERE category = 'test_rag_cycle'" 2>/dev/null || echo "Run seed script manually if PostgreSQL available"
    </automated>
  </verify>
  <acceptance_criteria>
    - database/tests/fixtures/seed_test_faq.sql existe e é SQL válido
    - Script possui DELETE para cleanup seguido de INSERT de 3 FAQs
    - category='test_rag_cycle' em todos os 3 INSERTs
    - Embeddings são cópias dos valores de test/fixtures/rag-test-knowledge.json
    - Script pode ser executado idempotentemente (DELETE antes do INSERT)
    - Comentário no topo documenta propósito e comando de cleanup
  </acceptance_criteria>
  <done>
Script SQL seed disponível para setup manual de fixtures RAG em ambientes de teste/dev.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → PostgreSQL | Test suite inserts FAQs with pre-computed embeddings (trusted test data) |
| Test suite → LLM API | Test query may leak to external LLM provider logs (Groq/OpenAI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Test fixtures in logs | low | accept | Test data is non-sensitive (public FAQ examples); no PII |
| T-02-02 | Tampering | npm/pip package installs | high | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit (langchain/core, langsmith, zod, faker-js/faker) |
| T-02-03 | Denial of Service | beforeAll timeout | medium | mitigate | jest.setTimeout(60000) prevents false negatives from slow boot |

</threat_model>

<verification>
**Phase gate:**
- npm run test:e2e -- --testPathPattern=rag-e2e-cycle exits 0
- Test output shows latency measurement < 3000ms
- PostgreSQL query log shows pgvector similarity search executed
</verification>

<success_criteria>
- ✅ Suite E2E verde com 1 test case de busca exata
- ✅ Fixtures de teste (JSON + SQL) criados e documentados
- ✅ Ciclo RAG provado: HTTP → pgvector → LLM → resposta contextualizada
- ✅ Latência medida e < 3s
- ✅ Cleanup de dados de teste no afterAll
</success_criteria>

<output>
Create `.planning/phases/02-validacao-e2e-texto-llm-rag/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Test files:**
- test/rag-e2e-cycle.e2e-spec.ts (suite E2E do tracer RAG)

**Fixtures:**
- test/fixtures/rag-test-knowledge.json (3 FAQs com embeddings 1536-dim)
- database/tests/fixtures/seed_test_faq.sql (script SQL seed para PostgreSQL)

**Test utilities:**
- TEST_FAQS fixture loader function (em rag-e2e-cycle.e2e-spec.ts)

**Test coverage:**
- RAG-01: POST /api/messages triggers RAG pipeline (HTTP → pgvector → LLM)
- RAG-04: Exact match query (similarity=1.0 or near-1.0)
