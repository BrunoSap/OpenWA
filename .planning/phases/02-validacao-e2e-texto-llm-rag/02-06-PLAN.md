---
phase: 02-validacao-e2e-texto-llm-rag
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/rag-e2e.yml
autonomous: false
gap_closure: true
requirements: [RAG-09]
user_setup:
  - service: github-actions
    why: "Verificar execução real do workflow em CI (services PostgreSQL+pgvector, seeding, testes) exige uma run no GitHub Actions, que só um humano com acesso ao repo pode acionar/observar"
    dashboard_config:
      - task: "Abrir a run do workflow 'RAG E2E Tests' e confirmar sucesso (services healthy, migrations, seed, testes verdes)"
        location: "GitHub → repo → Actions → RAG E2E Tests"
      - task: "(se LLM-as-judge exigir chave real) Confirmar OPENAI_API_KEY / GROQ_API_KEY em Secrets"
        location: "GitHub → repo → Settings → Secrets and variables → Actions"
estimate:
  tokens: 24000
  raw_tokens: 15000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Testes RAG E2E executam automaticamente em PRs via GitHub Actions (RAG-09) — workflow disparado e observado com sucesso"
    - "Service container PostgreSQL+pgvector sobe e o extension vector é criado"
    - "Fixtures (seed_test_faq.sql) semeiam antes dos testes rodarem"
  artifacts:
    - ".github/workflows/rag-e2e.yml validado (yaml lint + trigger real)"
    - "Log/URL da run do GitHub Actions confirmando sucesso"
  key_links:
    - "GitHub Actions → PostgreSQL service (pgvector/pgvector:pg16) → migrations → seed → testes"
---

<objective>
Fechar o Gap 2 (WARNING) da verificação da Fase 2: o workflow
`.github/workflows/rag-e2e.yml` (RAG-09) foi criado no plano 02-04 mas NUNCA foi
executado em CI. Portanto o service container PostgreSQL+pgvector, a criação da extension,
o seeding de fixtures e a execução dos testes permanecem não-verificados — podem falhar na
primeira run real por problemas de configuração.

Este plano valida o workflow em duas camadas: (1) validação estática local (lint YAML +
checagem de que os paths de trigger, comandos e nomes de arquivos referenciados existem de
fato no repo) que um agente executa de forma autônoma; e (2) uma verificação humana que
aciona a run real no GitHub Actions e confirma o resultado — algo que o agente não pode
fazer sozinho.

Purpose: Comprovar que RAG-09 (automação CI/CD) funciona de ponta a ponta, não apenas que o arquivo existe.
Output: Workflow validado estaticamente + evidência (URL/log) de uma run CI bem-sucedida.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-VERIFICATION.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-04-SUMMARY.md

@.github/workflows/rag-e2e.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validar o workflow estaticamente e provar que suas referências existem no repo</name>
  <files>.github/workflows/rag-e2e.yml</files>
  <precondition>`.github/workflows/rag-e2e.yml` existe (criado no plano 02-04).</precondition>
  <action>Validar o workflow sem depender de uma run CI. Passos: (1) parsear o YAML para
  garantir sintaxe válida (usar `node -e` com um parser YAML disponível, ou `python3 -c` com
  `yaml.safe_load`, conforme o que o ambiente tiver). (2) Confirmar que TODA referência do
  workflow existe no repo: o script `npm run test:e2e:rag` está definido em package.json; o
  arquivo `database/tests/fixtures/seed_test_faq.sql` existe; o script
  `database/tests/validate_rag_retrieval.py` existe; os paths do filtro de trigger
  (`test/rag-*.e2e-spec.ts`, `database/tests/validate_rag_retrieval.py`,
  `database/tests/fixtures/seed_test_faq.sql`, o próprio workflow) casam com arquivos reais.
  (3) Confirmar que o comando de migrations do workflow (`npm run migration:run`) existe em
  package.json e que o step de extension usa `CREATE EXTENSION IF NOT EXISTS vector` (idempotente).
  Se qualquer referência estiver quebrada, corrigir o workflow (ex.: ajustar path ou nome de
  script) — mas NÃO relaxar asserções de teste nem remover steps. Registrar quaisquer
  discrepâncias encontradas para o SUMMARY. Este check pega erros de configuração antes de
  gastar uma run de CI.</action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/rag-e2e.yml'))" &amp;&amp; node -e "const p=require('./package.json'); ['test:e2e:rag','migration:run'].forEach(s=>{if(!p.scripts[s])throw new Error('missing script '+s)})" &amp;&amp; test -f database/tests/fixtures/seed_test_faq.sql &amp;&amp; test -f database/tests/validate_rag_retrieval.py &amp;&amp; grep -q 'IF NOT EXISTS vector' .github/workflows/rag-e2e.yml</automated>
  </verify>
  <done>YAML parseia sem erro; scripts `test:e2e:rag` e `migration:run` existem em package.json; `seed_test_faq.sql` e `validate_rag_retrieval.py` existem; o step de extension é idempotente. Discrepâncias (se houver) corrigidas no workflow ou documentadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Acionar e observar a run real do workflow no GitHub Actions</name>
  <action>Peça ao humano para acionar a run real do workflow "RAG E2E Tests" e observar o
  resultado — isto é o que fecha definitivamente o Gap 2 (RAG-09), pois só uma execução real
  prova que os service containers, migrations, seeding e testes funcionam juntos no ambiente CI.

  Instruções para o humano:
  1. Acionar o workflow por um destes caminhos:
     - Abrir um PR que toque um path do filtro (ex.: editar `test/rag-e2e-cycle.e2e-spec.ts`
       ou o próprio `.github/workflows/rag-e2e.yml`), OU
     - GitHub → Actions → "RAG E2E Tests" → Run workflow (workflow_dispatch).
  2. Observar a run e confirmar, na ordem:
     - Service `postgres` (pgvector/pgvector:pg16) fica healthy.
     - Step "Create pgvector extension" sucede.
     - Step "Run database migrations" sucede.
     - Step "Seed test fixtures" sucede (3 FAQs em test_rag_cycle).
     - Step "Run RAG E2E tests" fica verde (incl. rag-llm-judge após o fix do plano 02-05;
       se OPENAI_API_KEY/GROQ_API_KEY não estiverem em Secrets, os testes RAG-03 fazem skip,
       o que é aceitável — anotar isso).
     - Step "Run Python RAG metrics" sucede (precision@k ok).
  3. Colar de volta: a URL da run e o status final de cada step (ou o trecho de log em caso de falha).
  4. Se algo falhar, colar o erro para diagnóstico e re-planejamento; NÃO marcar o gap como
     fechado com falhas.
  5. Após confirmação verde, decidir merge/fechamento do PR (se um PR foi usado para o trigger).

  Este checkpoint é bloqueante: sem a evidência da run o Gap 2 permanece aberto.</action>
  <verify>
    <human-check>Humano confirmou uma run do workflow "RAG E2E Tests" com todos os steps verdes (services healthy, extension criada, migrations, seed, testes, métricas) e forneceu a URL/log da run. Skips de RAG-03 por ausência de Secrets são aceitáveis e anotados.</human-check>
  </verify>
  <done>Existe evidência (URL + status dos steps) de uma run bem-sucedida do workflow em GitHub Actions, comprovando que PostgreSQL+pgvector sobe, extension é criada, fixtures semeiam e os testes RAG E2E executam automaticamente. RAG-09 satisfeito de ponta a ponta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR / evento GitHub → workflow | Entrada potencialmente não-confiável dispara execução de CI |
| CI runner → OpenAI/Groq API | Chaves de Secrets cruzam para provedores externos durante LLM-as-judge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | `.github/workflows/rag-e2e.yml` | medium | mitigate | Workflow em diretório protegido `.github/workflows/`; alteração exige permissão de escrita no repo; `permissions: contents: read` limita o token |
| T-02-10 | Information Disclosure | OPENAI_API_KEY / GROQ_API_KEY em logs | low | mitigate | Chaves vêm de GitHub Secrets (mascaradas nos logs); workflow não ecoa segredos |
| T-02-14 | Denial of Service | PR spam disparando CI | low | accept | Filtro de paths restringe triggers; `timeout-minutes: 15` evita jobs travados; limites de concorrência do GitHub Actions |
</threat_model>

<verification>
- YAML do workflow parseia sem erro (yaml.safe_load)
- Scripts referenciados (`test:e2e:rag`, `migration:run`) existem em package.json
- Arquivos referenciados (`seed_test_faq.sql`, `validate_rag_retrieval.py`) existem
- Step de extension é idempotente (`CREATE EXTENSION IF NOT EXISTS vector`)
- [HUMANO] Run real do GitHub Actions confirmada verde com URL/log, provando services + seed + testes
</verification>

<success_criteria>
- Gap 2 fechado: workflow validado estaticamente E executado com sucesso em CI real
- RAG-09 satisfeito de ponta a ponta (não apenas "arquivo existe")
- Evidência da run (URL + status dos steps) registrada no SUMMARY
- 1-2 commits: correções de validação estática (se necessárias) + documentação da verificação CI
</success_criteria>

<output>
Create `.planning/phases/02-validacao-e2e-texto-llm-rag/02-06-SUMMARY.md` when done
</output>
