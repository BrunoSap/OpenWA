---
phase: 02-validacao-e2e-texto-llm-rag
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
gap_closure: true
requirements: [RAG-03]
estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "LLM-as-judge evaluator valida que resposta usa contexto da KB (RAG-03) — o teste consegue importar ChatOpenAI sem erro de módulo"
    - "test/rag-llm-judge.e2e-spec.ts compila e resolve @langchain/openai em runtime"
  artifacts:
    - "package.json com @langchain/openai declarado em devDependencies"
    - "package-lock.json com @langchain/openai resolvido"
    - "node_modules/@langchain/openai instalado"
  key_links:
    - "test/rag-llm-judge.e2e-spec.ts (import ChatOpenAI) → @langchain/openai (pacote instalado)"
---

<objective>
Fechar o Gap 1 (BLOCKER) da verificação da Fase 2: o pacote `@langchain/openai` é
importado por `test/rag-llm-judge.e2e-spec.ts` (linha 12, `import { ChatOpenAI } from
'@langchain/openai'`) mas NÃO está declarado em `package.json` nem instalado em
`node_modules`. Isso bloqueia o requisito RAG-03 (validação de faithfulness via
LLM-as-judge) porque o teste falha em runtime com `Cannot find module '@langchain/openai'`.

Nota de root-cause: a VERIFICATION.md diz "declared but never installed", mas a inspeção
real mostra que `@langchain/core@1.2.9` está instalado enquanto `@langchain/openai` está
ausente de package.json por completo. O fix precisa ADICIONAR a dependência (versão
alinhada ao ecossistema @langchain/core 1.x) e instalá-la — não apenas rodar `npm install`.

Purpose: Desbloquear a execução do suite LLM-as-judge (RAG-03) sem alterar a lógica do teste.
Output: `@langchain/openai` declarado em devDependencies, lockfile atualizado, pacote instalado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-VERIFICATION.md
@.planning/phases/02-validacao-e2e-texto-llm-rag/02-02-SUMMARY.md

@package.json
@test/rag-llm-judge.e2e-spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Declarar e instalar @langchain/openai alinhado ao @langchain/core 1.x</name>
  <files>package.json, package-lock.json</files>
  <precondition>Rede disponível para o registro npm; @langchain/core@1.2.9 já instalado (confirma o ecossistema de versão 1.x).</precondition>
  <action>Adicionar `@langchain/openai` ao bloco `devDependencies` de package.json, na ordem
  alfabética junto aos outros pacotes `@langchain/*` (imediatamente após `@langchain/core`).
  Escolher a faixa de versão compatível com `@langchain/core` 1.x — o `@langchain/openai`
  depende de `@langchain/core` como peerDependency, então a major deve casar com a 1.x já
  presente. Usar `npm install @langchain/openai@^1 --save-dev` para resolver a versão exata
  contra o registro e gravar tanto package.json quanto package-lock.json. Se o resolver do npm
  reclamar de conflito de peer com @langchain/core@1.2.9, alinhar a faixa à versão publicada
  mais próxima que declare peer `@langchain/core` compatível com 1.2.x (verificar com
  `npm view @langchain/openai peerDependencies`). NÃO alterar `test/rag-llm-judge.e2e-spec.ts`
  — o import existente deve resolver sem mudanças. Isto fecha o BLOCKER que impede RAG-03.</action>
  <verify>
    <automated>npm ls @langchain/openai --depth=0 2>&1 | grep -q '@langchain/openai@' &amp;&amp; node -e "require.resolve('@langchain/openai')" &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies['@langchain/openai']) process.exit(1)"</automated>
  </verify>
  <done>`npm ls @langchain/openai --depth=0` mostra uma versão instalada, `require.resolve('@langchain/openai')` resolve sem erro, e package.json declara o pacote em devDependencies. package-lock.json contém a entrada resolvida.</done>
</task>

<task type="auto">
  <name>Task 2: Provar que o suite LLM-as-judge resolve o módulo e skipa sem OPENAI_API_KEY</name>
  <files>test/rag-llm-judge.e2e-spec.ts</files>
  <precondition>Task 1 concluída (@langchain/openai instalável); PostgreSQL com pgvector NÃO é necessário para este check porque o suite pula o boot quando OPENAI_API_KEY está ausente (beforeAll retorna cedo, ver linhas 61-66).</precondition>
  <action>Sem OPENAI_API_KEY definido, executar o suite LLM-as-judge para provar que (a) o import
  de `@langchain/openai` resolve em runtime (nenhum "Cannot find module") e (b) os dois testes
  fazem skip gracioso conforme o design documentado em 02-02-SUMMARY (Decision 2: graceful skip
  quando a chave não está definida). O objetivo é validar a resolução do módulo, não a chamada
  real à OpenAI. Rodar explicitamente com o env limpo para garantir o caminho de skip. Se o
  jest reportar erro de transform/import apontando para @langchain/openai, o Task 1 não foi
  concluído corretamente — corrigir a versão antes de prosseguir. NÃO adicionar OPENAI_API_KEY
  aqui; a validação faithfulness real acontece no CI (via GitHub Secrets) coberta pelo plano
  02-06. Documentar no SUMMARY que a chave é opcional para dev local e obrigatória apenas para
  a asserção real de faithfulness.</action>
  <verify>
    <automated>env -u OPENAI_API_KEY npx jest --config ./test/jest-e2e.json --testPathPattern='rag-llm-judge\.e2e-spec\.ts$' --runInBand 2>&1 | grep -Eq 'Tests:.*(passed|skipped)' &amp;&amp; ! (env -u OPENAI_API_KEY npx jest --config ./test/jest-e2e.json --testPathPattern='rag-llm-judge\.e2e-spec\.ts$' --runInBand 2>&1 | grep -q "Cannot find module '@langchain/openai'")</automated>
  </verify>
  <done>O suite `rag-llm-judge` roda até o fim sem o erro `Cannot find module '@langchain/openai'`. Sem OPENAI_API_KEY os dois testes RAG-03 passam pelo caminho de skip gracioso (log "⊘ Skipped"), confirmando que a resolução do módulo foi corrigida sem exigir chave em dev local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Instalação de pacote de terceiros cruza fronteira de confiança na build |
| test → OpenAI API | Chave e prompt cruzam para provedor externo (só quando OPENAI_API_KEY definido, fora deste plano) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `npm install @langchain/openai` | high | mitigate | Pacote legítimo publicado pela org `@langchain` (mesma org de `@langchain/core@1.2.9` já em uso e verificado na Fase 2); instalar faixa alinhada à major 1.x já presente; lockfile fixa a resolução |
| T-02-13 | Information Disclosure | OPENAI_API_KEY | low | accept | Este plano NÃO define a chave; caminho de skip é exercitado sem segredo. Validação real com chave só em CI (plano 02-06) via GitHub Secrets mascarados |
</threat_model>

<verification>
- `npm ls @langchain/openai --depth=0` lista uma versão instalada
- `node -e "require.resolve('@langchain/openai')"` resolve sem erro
- package.json declara `@langchain/openai` em devDependencies (ordem alfabética junto a @langchain/core)
- package-lock.json contém a entrada resolvida do pacote
- `env -u OPENAI_API_KEY` + suite rag-llm-judge roda sem `Cannot find module '@langchain/openai'` e faz skip gracioso
- Nenhuma mudança na lógica de `test/rag-llm-judge.e2e-spec.ts`
</verification>

<success_criteria>
- Gap 1 fechado: `@langchain/openai` declarado e instalado
- RAG-03 desbloqueado: o suite LLM-as-judge consegue importar ChatOpenAI
- Contrato de skip preservado: sem OPENAI_API_KEY os testes pulam, não falham
- 1 commit: atualização de package.json + package-lock.json
</success_criteria>

<output>
Create `.planning/phases/02-validacao-e2e-texto-llm-rag/02-05-SUMMARY.md` when done
</output>
