---
phase: 04-implementa-o-e2e-vision
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/support/vision-analyze.ts
  - test/vision-accuracy.e2e-spec.ts
  - test/fixtures/images/document-scan-expected.json
  - test/fixtures/images/scene-photo-expected.json
autonomous: true
requirements: [VIS-06, VIS-07, VIS-09]

estimate:
  tokens: 55000
  raw_tokens: 40000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Caso documento/OCR: imagem com texto é analisada via Vision e a descrição contém o texto/conteúdo esperado, validado por LLM-as-judge (score >= minSimilarity) — VIS-06, VIS-09"
    - "Caso screenshot/cena: imagem de ambiente é analisada e a descrição captura os elementos-chave, validado por LLM-as-judge — VIS-07, VIS-09"
    - "A acurácia da descrição é validada por similaridade semântica (LLM-as-judge), não por matching exato de string — VIS-09"
    - "Quando a análise Vision falha (timeout ou erro de API), o helper retorna um resultado de fallback determinístico com mensagem ao usuário, sem lançar exceção"
  artifacts:
    - test/support/vision-analyze.ts
    - test/vision-accuracy.e2e-spec.ts
    - test/fixtures/images/document-scan-expected.json
    - test/fixtures/images/scene-photo-expected.json
  key_links:
    - "descrição do Vision + expectedDescription do fixture → semanticSimilarity (LLM-as-judge gpt-4o-mini) → score >= minSimilarity"
    - "analyzeImage → analyzeWithFallback (Promise.race com timeout) → { ok, fallbackReason } sem exceção"
    - "fallbackReason → buildFallbackReply → mensagem PT ao usuário"
---

<objective>
Expandir a validação Vision além do tracer: adicionar os casos documento/OCR (VIS-06) e screenshot/cena (VIS-07), validar a acurácia das descrições por LLM-as-judge de similaridade semântica (VIS-09), e adicionar tratamento de erro/fallback ao helper (análogo ao `transcribeWithFallback` da Fase 3).

Purpose: Cobrir os casos restantes exigidos pela fase e provar a qualidade semântica das descrições sem depender de matching exato (outputs Vision são não-determinísticos). Fecha os deliverables "cobertura: produto, documento, screenshot, ambiente" e "validação de acurácia" da fase. Reutiliza integralmente o helper e os padrões do tracer (Plan 01) e o padrão LLM-as-judge da Fase 2.

Output: Suite `vision-accuracy.e2e-spec.ts` verde com casos documento e cena validados por LLM-as-judge; fixtures dos dois casos; funções `semanticSimilarity`, `analyzeWithFallback` e `buildFallbackReply` adicionadas ao helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md
@.planning/phases/04-implementa-o-e2e-vision/04-01-SUMMARY.md
@test/support/stt-transcribe.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LLM-as-judge + fallback no helper Vision</name>
  <files>test/support/vision-analyze.ts</files>
  <read_first>
    - test/support/vision-analyze.ts (helper criado no Plan 01 — estender, não reescrever `analyzeImage`)
    - test/support/stt-transcribe.ts L166-239 (padrão de `transcribeWithFallback` com Promise.race + timeout e `buildFallbackReply` — espelhar para Vision)
    - .planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md L271-311 (semanticSimilarity com LLM-as-judge, prompt de grading e retorno JSON) e L794-845
    - test/rag-llm-judge.e2e-spec.ts (padrão LLM-as-judge da Fase 2 já usado no projeto)
  </read_first>
  <action>
Estender `test/support/vision-analyze.ts` com validação semântica e fallback. NÃO alterar a assinatura de `analyzeImage` já criada no Plan 01.

**`semanticSimilarity(expected: string, actual: string): Promise<{ score: number; explanation: string }>`:**
- Ler chave só de `process.env.OPENAI_API_KEY`; lançar erro claro se ausente (o teste faz skip).
- Usar `ChatOpenAI` gpt-4o-mini, `temperature: 0`. Prompt de grading (espelhar RESEARCH L287-301): compara duas descrições de imagem e retorna score em [0.0, 1.0] com faixas documentadas, focando em elementos visuais-chave, mesmo assunto e detalhes (cores, objetos, texto). Responder JSON `{ "score": number, "explanation": string }`.
- Fazer parse robusto do JSON da resposta (tolerar cercas de código markdown do tipo json que o modelo às vezes adiciona, removendo-as antes do parse); em falha de parse, lançar erro claro. Retornar `{ score, explanation }`.

**`analyzeWithFallback(imageBuffer: Buffer, opts: { prompt?: string; model?: string; detail?: 'low'|'high'|'auto'; timeoutMs?: number }): Promise<{ description: string; latencyMs: number; ok: boolean; fallbackReason?: 'timeout' | 'api_error' }>`:**
- Espelhar `transcribeWithFallback`: `timeoutMs` default 10000; `Promise.race` entre `analyzeImage(...)` e um timeout que rejeita com `Error('TIMEOUT')`.
- Em sucesso: `{ description, latencyMs, ok: true }`. Em `TIMEOUT`: `{ description: '', latencyMs, ok: false, fallbackReason: 'timeout' }`. Em qualquer outro erro (inclusive formato inválido / erro de API): `{ description: '', latencyMs, ok: false, fallbackReason: 'api_error' }`. NÃO lançar exceção.

**`buildFallbackReply(fallbackReason: 'timeout' | 'api_error'): string`:**
- Espelhar o equivalente STT: mensagem PT determinística. `timeout` → pedir para reenviar (tempo esgotado); `api_error` → pedir para reenviar (erro ao processar a imagem). Manter pura para teste isolado.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c 'vision-analyze' | { read n; test "$n" = "0"; }</automated>
    <automated>node -r ts-node/register -e "const m=require('./test/support/vision-analyze.ts'); if(typeof m.semanticSimilarity!=='function'||typeof m.analyzeWithFallback!=='function'||typeof m.buildFallbackReply!=='function')process.exit(1); const t=m.buildFallbackReply('timeout'); const e=m.buildFallbackReply('api_error'); if(!t||!e||t===e)process.exit(1); console.log('vision fallback fns ok')"</automated>
    <automated>node -r ts-node/register -e "const {analyzeWithFallback}=require('./test/support/vision-analyze.ts'); (async()=>{ const r=await analyzeWithFallback(Buffer.from([1,2,3,4]), { timeoutMs: 500 }); if(r.ok!==false||!r.fallbackReason)process.exit(1); console.log('fallback on bad input ok:', r.fallbackReason); })().catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>Helper exporta `semanticSimilarity`, `analyzeWithFallback` e `buildFallbackReply`; compila sem erro; `buildFallbackReply` retorna mensagens PT distintas por motivo; `analyzeWithFallback` com buffer inválido retorna `ok:false` com `fallbackReason` sem lançar exceção.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fixtures documento/OCR e screenshot/cena</name>
  <files>test/fixtures/images/document-scan-expected.json, test/fixtures/images/scene-photo-expected.json</files>
  <read_first>
    - test/fixtures/images/product-photo-expected.json (formato de fixture criado no Plan 01 — mesmas chaves)
    - test/fixtures/images/README.md (documentação de fixtures do Plan 01 — atualizar com os novos casos)
    - .planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md L847-861 (formato de metadados de fixture)
  </read_first>
  <action>
Criar os metadados dos dois novos casos, mesmo formato do `product-photo-expected.json` (chaves `imageFile`, `format`, `sizeBytes`, `expectedDescription`, `minSimilarity`, `visualElements`).

- `document-scan-expected.json` (VIS-06 documento/OCR): imagem com texto legível (ex.: documento/recibo/screenshot de texto). `expectedDescription` deve descrever o conteúdo E citar o texto-chave esperado (OCR). Como texto pequeno pode exigir mais detalhe, documentar no README que este caso pode usar `detail: 'high'` se `low` falhar (A4 do RESEARCH). `minSimilarity` 0.7.
- `scene-photo-expected.json` (VIS-07 cena/ambiente): imagem de cena/ambiente/pessoa. `expectedDescription` descreve os elementos da cena. `minSimilarity` 0.7.

**Binários das imagens:** o executor deve prover os dois arquivos de imagem (~50KB cada, JPEG/PNG/WebP, domínio público, conteúdo verificado) em `test/fixtures/images/` e referenciá-los pelos respectivos `imageFile`. Se algum binário não puder ser incluído, registrar no README e o caso correspondente pulará com aviso (mesmo padrão de skip por placeholder < 1KB). Atualizar `test/fixtures/images/README.md` listando os três casos (produto, documento, cena), fonte e nota de custo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['document-scan-expected','scene-photo-expected']){ const m=JSON.parse(fs.readFileSync('./test/fixtures/images/'+f+'.json','utf8')); if(!m.imageFile||!m.expectedDescription||typeof m.minSimilarity!=='number'||!Array.isArray(m.visualElements))process.exit(1);} console.log('vision fixtures meta ok')"</automated>
  </verify>
  <done>`document-scan-expected.json` e `scene-photo-expected.json` existem com o mesmo formato do fixture de produto (imageFile/expectedDescription/minSimilarity/visualElements); README atualizado com os três casos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Suite de acurácia Vision (documento + cena via LLM-as-judge)</name>
  <files>test/vision-accuracy.e2e-spec.ts</files>
  <read_first>
    - test/vision-e2e-cycle.e2e-spec.ts (tracer do Plan 01 — mesmo scaffolding de skip, boot e logs)
    - test/rag-llm-judge.e2e-spec.ts (padrão LLM-as-judge da Fase 2)
    - test/support/vision-analyze.ts (analyzeImage + semanticSimilarity + analyzeWithFallback do Task 1)
    - test/fixtures/images/document-scan-expected.json, test/fixtures/images/scene-photo-expected.json (Task 2)
  </read_first>
  <action>
Criar a suite de acurácia semântica cobrindo os casos documento/OCR (VIS-06) e cena (VIS-07), validando por LLM-as-judge (VIS-09).

**Estrutura (espelhar vision-e2e-cycle):**
- `jest.mock('archiver', ...)` na primeira linha; `jest.setTimeout(60000)`.
- `beforeAll`: skip gracioso se `OPENAI_API_KEY` ausente. Carregar os dois fixtures JSON e ler os buffers das imagens; para cada caso cujo binário não existe ou é placeholder (< 1000 bytes), marcar skip individual daquele caso (não da suite inteira).

**Para cada caso (documento, cena):**
- Chamar `analyzeImage(buffer, { model: 'gpt-4o-mini', detail })` — usar `detail` conforme o fixture recomendar (documento pode usar `high`).
- Chamar `semanticSimilarity(fixture.expectedDescription, resultado.description)`; asserir `score >= fixture.minSimilarity`; logar expected/actual/score/explanation e custo.

**Caso de fallback (guarda o comportamento de erro):**
- Um `it` determinístico (roda mesmo sem chave): chamar `analyzeWithFallback` com um buffer inválido/formato não suportado e `timeoutMs` curto; asserir `ok === false` e `fallbackReason` definido; asserir que `buildFallbackReply(fallbackReason)` retorna string não-vazia em PT. Este caso não deve depender de rede e não deve pular por ausência de chave.

Todos os `it` que chamam a API iniciam com guard de skip. Não usar matching exato de string para descrições Vision (anti-pattern do RESEARCH L600) — apenas LLM-as-judge.
  </action>
  <verify>
    <automated>npm run test:e2e -- --testPathPatterns='vision-accuracy\.e2e-spec\.ts$' --runInBand 2>&1 | tail -30</automated>
  </verify>
  <done>Suite `vision-accuracy.e2e-spec.ts` roda verde: com OPENAI_API_KEY + imagens, os casos documento e cena atingem score de similaridade >= minSimilarity via LLM-as-judge; o caso de fallback passa determinístico (ok:false + reply PT) independente de chave; sem chave/imagens, os casos de API pulam com aviso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/CI → OpenAI API | OPENAI_API_KEY (Vision + judge) cruza para serviço externo; env/secret, nunca literal |
| fixture de imagem → Vision API | Imagens de teste controladas; formato validado antes de submeter (helper do Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | OPENAI_API_KEY em teste/fixtures | high | mitigate | Chave só de `process.env.OPENAI_API_KEY`; nenhum literal; skip gracioso |
| T-04-03 | Denial of Service | custo de chamadas judge + Vision extras | medium | mitigate | gpt-4o-mini para Vision e judge; `detail: 'low'` por padrão; fixtures < 500KB; casos pulam sem chave |
| T-04-05 | Tampering | descrições esperadas nos fixtures | low | accept | expectedDescription revisada manualmente; validação por similaridade tolera fraseado |
</threat_model>

<verification>
- `npm run test:e2e:vision` roda tracer (Plan 01) + acurácia (este plan): verde com chave+imagens; skip por caso caso contrário.
- Caso de fallback passa determinístico sem rede.
- Nenhuma chave literal nas suites/fixtures; sem matching exato de string para descrições.
</verification>

<success_criteria>
- Casos documento/OCR (VIS-06) e cena (VIS-07) cobertos e validados por LLM-as-judge (VIS-09).
- Helper estendido com `semanticSimilarity`, `analyzeWithFallback`, `buildFallbackReply` reutilizáveis.
- Comportamento de erro/fallback provado determinístico.
</success_criteria>

<output>
Create `.planning/phases/04-implementa-o-e2e-vision/04-02-SUMMARY.md` when done
</output>
