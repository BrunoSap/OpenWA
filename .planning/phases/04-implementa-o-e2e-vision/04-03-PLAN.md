---
phase: 04-implementa-o-e2e-vision
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - WhatsApp-Vision-Analysis.json
  - test/vision-workflow-shape.e2e-spec.ts
  - .github/workflows/vision-e2e.yml
  - docs/GUIDES.md
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-10, VIS-11]

estimate:
  tokens: 48000
  raw_tokens: 36000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Existe o workflow n8n WhatsApp-Vision-Analysis.json: JSON válido, com webhook de entrada, download de imagem, nó de Vision e nó de LLM contextualizado, e envio de resposta ao WhatsApp — VIS-01, VIS-02, VIS-03, VIS-04"
    - "O workflow não contém segredos literais; credenciais são referenciadas via $env — VIS-01"
    - "As suites Vision rodam automaticamente no CI (GitHub Actions) em PRs que tocam arquivos de imagem/Vision — VIS-11"
    - "O custo por imagem e o modo de detalhe (low/high) estão documentados em GUIDES.md — VIS-10"
  artifacts:
    - WhatsApp-Vision-Analysis.json
    - test/vision-workflow-shape.e2e-spec.ts
    - .github/workflows/vision-e2e.yml
    - docs/GUIDES.md
  key_links:
    - "workflow JSON → shape test (JSON válido + webhook + download + Vision + LLM + envio + grep negativo de segredos)"
    - "GitHub Actions → npm run test:e2e:vision → suites tracer + acurácia + fallback"
    - "OPENAI_API_KEY via secrets do CI → env do job (skip gracioso quando ausente)"
    - "seção de custos Vision em GUIDES.md → preços gpt-4o-mini + detail low/high"
---

<objective>
Criar o workflow n8n `WhatsApp-Vision-Analysis.json` (imagem WhatsApp → download → GPT-4 Vision → LLM contextualizado → resposta), validar sua forma (shape) por teste, integrar as suites Vision ao CI/CD, e documentar os custos em GUIDES.md — reutilizando os padrões da Fase 3 (shape test + GitHub Actions).

Purpose: Fechar os deliverables da fase que faltam ("workflow n8n importável e configurável", "testes rodam no CI", "documentação de custos e rate limits"). O workflow n8n de Vision está ausente hoje (só existem workflows unified/áudio/intake); este plan o cria e o valida estruturalmente sem segredos, e amarra a validação Vision ao pipeline automático.

Output: Workflow n8n de Vision versionado + suite de shape verde + GitHub Actions dedicado às suites Vision + seção de custos Vision em GUIDES.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md
@.planning/phases/04-implementa-o-e2e-vision/04-01-SUMMARY.md
@.planning/phases/04-implementa-o-e2e-vision/04-02-SUMMARY.md
@WhatsApp-Audio-Transcription.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Workflow n8n WhatsApp-Vision-Analysis.json</name>
  <files>WhatsApp-Vision-Analysis.json</files>
  <read_first>
    - WhatsApp-Audio-Transcription.json (workflow real da Fase 3: estrutura name/nodes/connections, webhook path, nó de código, LLM, httpRequest de envio via /messages/send-text, uso de $env para credenciais) — espelhar trocando áudio por imagem
    - .planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md L505-596 (Pattern 3: shape do workflow Vision — webhook, download de imagem, convert to base64, GPT-4 Vision, LLM contextualizado, send reply)
    - docs/GUIDES.md L535-683 (contrato Vision documentado: rota de mídia, modelos, exemplos)
  </read_first>
  <action>
Criar o workflow n8n `WhatsApp-Vision-Analysis.json` no root do repo, espelhando a estrutura de `WhatsApp-Audio-Transcription.json` e o Pattern 3 do RESEARCH.

**Nós (mínimo):**
- Webhook de entrada (`n8n-nodes-base.webhook`) com path para imagem (ex.: `whatsapp-vision`).
- Download da imagem (`n8n-nodes-base.httpRequest`, GET em `={{$json.mediaUrl}}`, encoding arraybuffer).
- Conversão para base64 (nó de código/function): monta `data:${mimeType};base64,${base64}` e propaga `chatId`.
- Nó Vision (LLM langchain com modelo `gpt-4o-mini`, content block de imagem) que produz a descrição.
- Nó LLM contextualizado (langchain `gpt-4o-mini`) que gera a resposta ao usuário a partir da descrição.
- Envio da resposta (`n8n-nodes-base.httpRequest`, POST) cujo `url` referencia a rota de envio ao WhatsApp (`/messages/send-text`, mesmo padrão do workflow de áudio).

**Conexões:** encadear Webhook → Download → Base64 → Vision → LLM → Send (linear), espelhando o bloco `connections` do RESEARCH L578-594.

**Segurança (guarda T-04-01):** nenhuma credencial literal. Toda chave/baseURL via `$env` (ex.: `$env.OPENAI_API_KEY`, `$env.API_BASE_URL`). Não embutir tokens.

Disciplina de comentário: não escrever por extenso, em `<action>`, o valor literal de qualquer prefixo de chave que o shape test venha a negativar; referir-se por descrição.
  </action>
  <verify>
    <automated>node -e "const w=JSON.parse(require('fs').readFileSync('./WhatsApp-Vision-Analysis.json','utf8')); if(typeof w.name!=='string'||!Array.isArray(w.nodes)||w.nodes.length===0||!w.connections)process.exit(1); const types=w.nodes.map(n=>n.type||''); if(!types.some(t=>t.includes('webhook')))process.exit(1); if(!types.some(t=>t.toLowerCase().includes('httprequest')))process.exit(1); console.log('vision workflow shape ok:', w.nodes.length,'nodes')"</automated>
  </verify>
  <done>`WhatsApp-Vision-Analysis.json` é JSON válido com name/nodes/connections, contém webhook + httpRequest de download + nó base64 + nó Vision + nó LLM + httpRequest de envio, encadeados linearmente, e nenhuma credencial literal (só referências $env).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Teste de shape do workflow n8n de Vision</name>
  <files>test/vision-workflow-shape.e2e-spec.ts</files>
  <read_first>
    - test/audio-workflow-shape.e2e-spec.ts (padrão de shape test da Fase 3: readFileSync + JSON.parse em beforeAll, asserts em name/nodes/connections, webhook presente, httpRequest referenciando rota, grep negativo de segredos literais e credenciais via $env) — espelhar trocando áudio por Vision
    - test/intake-workflow-shape.e2e-spec.ts (mesmo padrão de shape)
    - WhatsApp-Vision-Analysis.json (workflow criado no Task 1)
  </read_first>
  <action>
Criar a suite de shape do workflow de Vision espelhando `audio-workflow-shape.e2e-spec.ts`.

**Carregamento:** `readFileSync` de `WhatsApp-Vision-Analysis.json` no root; `JSON.parse` uma vez em `beforeAll`.

**Asserts de shape:**
- Parseia como JSON válido; `name` string; `nodes` array não-vazio; `connections` objeto não-nulo.
- Tem pelo menos um nó webhook de entrada (`n8n-nodes-base.webhook`) — VIS-01.
- Tem um nó httpRequest de download de imagem (GET referenciando `mediaUrl`) — VIS-02.
- Tem um nó de Vision (LLM langchain com content de imagem) — VIS-03.
- Tem um nó de LLM contextualizado que consome a descrição — VIS-04.
- Tem um nó httpRequest de envio cujo `url` referencia a rota de envio ao WhatsApp (`/messages/send-text`).

**Asserts de segurança (grep negativo — guardam T-04-01):**
- O JSON serializado NÃO contém chaves literais: negativar padrão de chave OpenAI (prefixo de chave secreta) e Bearer com token literal.
- Asserir que a serialização referencia credenciais via `$env` e não valores embutidos.

IMPORTANTE (disciplina de comentário): construir os padrões negativos via variáveis/regex montadas, não como string proibida escrita por extenso no `<action>`/comentário.
  </action>
  <verify>
    <automated>npx jest --config ./test/jest-e2e.json --testPathPatterns='vision-workflow-shape\.e2e-spec\.ts$' --runInBand 2>&1 | tail -25</automated>
  </verify>
  <done>Suite `vision-workflow-shape.e2e-spec.ts` verde: workflow parseia como JSON válido, tem webhook + download + Vision + LLM + httpRequest para /messages/send-text, e nenhum segredo literal (só referências $env).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GitHub Actions para suites Vision + docs de custo em GUIDES.md</name>
  <files>.github/workflows/vision-e2e.yml, docs/GUIDES.md</files>
  <read_first>
    - .github/workflows/audio-stt-e2e.yml (padrão de CI da Fase 3: triggers por paths, services postgres+redis, setup node, npm ci, env com chave via secrets, upload de artefatos, SHAs pinados) — espelhar trocando STT por Vision
    - .github/workflows/rag-e2e.yml (referência dos service containers e steps de migração)
    - docs/GUIDES.md L535-683 (seção Vision existente — adicionar subseção de custos/rate limits perto dela)
    - .planning/phases/04-implementa-o-e2e-vision/04-RESEARCH.md L644-682 (dados de custo: gpt-4o-mini $0.15/$0.60 por 1M, detail low = 85 tokens, high tile-based)
  </read_first>
  <action>
**GitHub Actions `.github/workflows/vision-e2e.yml`** (espelhar `audio-stt-e2e.yml`), VIS-11:
- Triggers (`on`): `pull_request` (branches main, develop) com `paths`: `test/vision-*.e2e-spec.ts`, `test/support/vision-analyze.ts`, `test/fixtures/images/**`, `WhatsApp-Vision-Analysis.json`, `.github/workflows/vision-e2e.yml`. `push` em main; `workflow_dispatch`.
- Job `vision-e2e`: `runs-on: ubuntu-latest`, `timeout-minutes: 15`, `permissions: contents: read`. Reusar os service containers postgres (pgvector/pgvector:pg16) e redis do padrão STT/RAG (as suites bootam o AppModule que exige DB/Redis). Steps: checkout, setup-node (v22, cache npm), `npm ci`, steps de extensão/migração reusados do rag/stt conforme necessário para boot.
- Step principal: `npm run test:e2e:vision` com env `NODE_ENV=test`, `DATABASE_*`, `REDIS_*` (mesmos do stt/rag) e `OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}`.
- Comentário no YAML: casos que exigem Vision real pulam graciosamente quando `OPENAI_API_KEY`/imagens ausentes; o caso de fallback (Plan 02) roda determinístico sempre.
- Upload de artefatos (test-results/, coverage/) com `if: always()`. Pinar todas as actions por SHA (mesmo estilo do audio-stt-e2e.yml).

**Docs de custo em `docs/GUIDES.md`** (VIS-10): adicionar, junto à seção Vision existente (L535-683), uma subseção "Custos e rate limits Vision" documentando: preço gpt-4o-mini ($0.15/1M entrada, $0.60/1M saída) vs gpt-4o ($2.50/$10.00), o efeito do modo `detail` (low = 85 tokens fixos por imagem; high = tile-based ~170 tokens/tile 512px), recomendação de usar gpt-4o-mini + detail low para testes, e que o custo por imagem é medido e logado pelos testes (helper `analyzeImage`). Referenciar o número de custo real observado nos SUMMARYs dos Plans 01/02 se disponível.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/vision-e2e.yml','utf8'); const need=['test:e2e:vision','OPENAI_API_KEY','pull_request','workflow_dispatch','vision-']; for(const n of need){ if(!y.includes(n)){ console.error('missing',n); process.exit(1);} } console.log('vision ci ok')"</automated>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/vision-e2e.yml')); print('yaml valid')"</automated>
    <automated>node -e "const g=require('fs').readFileSync('docs/GUIDES.md','utf8'); if(!/gpt-4o-mini/.test(g)||!/detail/i.test(g)){process.exit(1)} console.log('guides cost docs ok')"</automated>
  </verify>
  <done>`.github/workflows/vision-e2e.yml` é YAML válido, dispara em PR/push/dispatch nos paths de Vision, roda `npm run test:e2e:vision` com `OPENAI_API_KEY` via secrets, faz upload de artefatos e pina actions por SHA; GUIDES.md tem subseção de custos Vision citando gpt-4o-mini e o modo detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow JSON → repositório | Workflow versionado não pode conter credenciais literais |
| CI → OpenAI API | OPENAI_API_KEY injetada via `secrets` do GitHub, nunca em texto no YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | segredos literais em WhatsApp-Vision-Analysis.json | high | mitigate | Shape test faz grep negativo de padrões de chave (OpenAI/Bearer) e exige referências $env |
| T-04-06 | Elevation of Privilege | permissões amplas do job CI | medium | mitigate | `permissions: contents: read`; actions pinadas por SHA |
| T-04-07 | Tampering | actions de terceiros no CI | medium | mitigate | Todas as actions pinadas por SHA de commit, espelhando audio-stt-e2e.yml |
</threat_model>

<verification>
- Workflow n8n de Vision criado e validado estruturalmente (webhook + download + Vision + LLM + envio) e livre de segredos literais.
- `.github/workflows/vision-e2e.yml` é YAML válido, roda `test:e2e:vision`, injeta OPENAI_API_KEY via secrets, pina actions por SHA.
- GUIDES.md documenta custos e modo detail (VIS-10).
- CI dispara nos paths corretos de imagem/Vision (VIS-11).
</verification>

<success_criteria>
- Workflow n8n de Vision importável e livre de segredos (VIS-01/02/03/04, guarda T-04-01).
- Suites Vision (tracer + acurácia + fallback) rodam automaticamente no CI em PRs de imagem (VIS-11).
- Documentação de custos/rate limits presente em GUIDES.md (VIS-10).
- Padrões da Fase 3 (services, pinning, upload de artefatos) reutilizados.
</success_criteria>

<output>
Create `.planning/phases/04-implementa-o-e2e-vision/04-03-SUMMARY.md` when done
</output>
