---
phase: 06-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/modules/analytics/services/analytics-cost.util.ts
  - src/modules/analytics/services/percentile.util.ts
  - src/modules/analytics/listeners/analytics-event.listener.ts
  - src/modules/llm/llm.service.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-05]

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five business events (conversation.started, conversation.resolved, conversation.escalated, llm.called, fallback.triggered) are recorded to analytics_events when ANALYTICS_ENABLED=true"
    - "llm.called carries a cost_usd computed from provider+tokens via analytics-cost.util (Groq=0, OpenAI gpt-4o-mini priced per RESEARCH §3.3)"
  artifacts:
    - src/modules/analytics/services/analytics-cost.util.ts
    - src/modules/analytics/services/percentile.util.ts
    - src/modules/analytics/listeners/analytics-event.listener.ts
  key_links:
    - "llm.service.ts emits 'llm.called' with tokens+provider -> listener computes cost via analytics-cost.util -> persisted cost_usd consumed downstream by 06-02b aggregation and /cost endpoint"
    - "each new @OnEvent handler reuses the existing 06-01 `enabled` gate: disabled -> recordEvent never called"
---

<objective>
Expand the proven 06-01 tracer with the remaining domain-event emission and the pure metric utilities: emit the five business events (`conversation.started`, `conversation.resolved`, `conversation.escalated`, `llm.called`, `fallback.triggered`), and provide the cost + percentile calculation utilities that the aggregation layer (06-02b) and query endpoints will consume.

Purpose: Deliver the event-collection half of the ROADMAP "backend: coletor de métricas" plus the reusable cost/percentile math. Builds directly on the 06-01 slice (entity, service, listener, endpoint) — each event added here is horizontal expansion from that vertical path. Aggregation, storage rollups, and query endpoints follow in 06-02b, which depends on the events and utils established here.

Output: cost + percentile utils with co-located specs; five more `@OnEvent` handlers on the existing listener; one `llm.called` emit site in `llm.service.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-analytics-dashboard/06-RESEARCH.md
@.planning/phases/06-analytics-dashboard/06-01-SUMMARY.md

# Reference patterns:
@src/modules/memory/memory.module.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cost + percentile utils (pure math, unit-proven)</name>
  <files>src/modules/analytics/services/analytics-cost.util.ts, src/modules/analytics/services/percentile.util.ts</files>
  <read_first>
    - .planning/phases/06-analytics-dashboard/06-RESEARCH.md §3.3 (exact pricing constants: Groq $0; OpenAI gpt-4o-mini $0.15/1M input, $0.60/1M output, +$0.001/image)
    - .planning/phases/06-analytics-dashboard/06-RESEARCH.md §3.4 (percentile usage in aggregation — p50/p95/p99 over latency arrays)
  </read_first>
  <behavior>
    - calculateCost({provider:'groq',...}) returns 0
    - calculateCost({provider:'openai', model:'gpt-4o-mini', tokens_input:1_000_000, tokens_output:0}) returns 0.15
    - calculateCost with images_count:2 adds 0.002
    - calculateCost with an unknown provider returns 0
    - percentile([10,20,30,40], 0.5) interpolates to 25; p95/p99 return near-max values; empty array returns null
  </behavior>
  <action>Create `analytics-cost.util.ts` exporting `calculateCost(event): number` implementing RESEARCH.md §3.3 exactly: groq -> 0; openai -> (tokens_input/1_000_000)*0.15 + (tokens_output/1_000_000)*0.60 + (images_count||0)*0.001; unknown provider -> 0. Define the per-1M rates as named constants at the top of the file. Create `percentile.util.ts` exporting `percentile(values: number[], q): number|null` (sort ascending, linear interpolation between ranks, null on empty). Write co-located `*.util.spec.ts` first (RED) asserting the cost and percentile behaviors above.</action>
  <verify>
    <automated>npx jest src/modules/analytics/services/analytics-cost.util.spec.ts src/modules/analytics/services/percentile.util.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - analytics-cost.util.ts contains `calculateCost(` and named rate constants; spec asserts groq=0, gpt-4o-mini 1M input=0.15, image add of 0.001 each, unknown provider=0
    - percentile.util.ts contains `percentile(`; spec asserts p50 interpolation and empty->null
    - both util specs exit 0
  </acceptance_criteria>
  <done>Cost and percentile math are unit-proven and available for the aggregation layer (06-02b) to import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit remaining 5 events + llm.called cost tracking on the listener</name>
  <files>src/modules/analytics/listeners/analytics-event.listener.ts, src/modules/llm/llm.service.ts</files>
  <read_first>
    - src/modules/analytics/listeners/analytics-event.listener.ts (from 06-01 — add the new @OnEvent handlers next to message.processed, reuse the `enabled` gate and recordEvent mapping)
    - src/modules/llm/llm.service.ts (locate the method that calls Groq/OpenAI and read the response usage object shape — prompt_tokens/completion_tokens — to emit llm.called; if the file path differs, grep src/modules for the LLM call site and use the actual path)
    - src/modules/analytics/services/analytics-cost.util.ts (Task 1 — import calculateCost for the llm.called handler)
    - .planning/phases/06-analytics-dashboard/06-RESEARCH.md §3.1-3.3 and §7.1 (event->column mapping for conversation.*, fallback.triggered, llm.called)
  </read_first>
  <behavior>
    - each of the 5 new @OnEvent handlers early-returns when analytics disabled and records the mapped row when enabled
    - the llm.called handler computes cost_usd via calculateCost and sets tokens_used = tokens_input + tokens_output
    - llm.service.ts emits 'llm.called' with { provider, model, tokens_input, tokens_output, latency_ms } after each LLM response
  </behavior>
  <action>In analytics-event.listener.ts add `@OnEvent` handlers for `conversation.started`, `conversation.resolved`, `conversation.escalated`, `fallback.triggered`, and `llm.called` — each gated by the existing `enabled` getter and calling recordEvent with the mapped columns (session_id, chat_id, user_id, conversation_id, payload); for `llm.called` compute cost_usd via calculateCost (imported from analytics-cost.util) and set tokens_used = tokens_input+tokens_output. In src/modules/llm/llm.service.ts inject `EventEmitter2` and after each LLM response emit `llm.called` with { provider, model, tokens_input, tokens_output, latency_ms }. Extend the listener spec (or add one) asserting each handler is a no-op when disabled and records the mapped row when enabled, and that llm.called persists a computed cost_usd.</action>
  <verify>
    <automated>ANALYTICS_ENABLED=true npx jest --config ./test/jest-e2e.json --testPathPatterns='analytics-tracer\.e2e-spec\.ts$' --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - analytics-event.listener.ts contains `@OnEvent('llm.called')`, `@OnEvent('fallback.triggered')`, `@OnEvent('conversation.started')`, `@OnEvent('conversation.resolved')`, `@OnEvent('conversation.escalated')`
    - llm.service.ts emits `'llm.called'`
    - the llm.called handler imports and calls calculateCost from analytics-cost.util
    - the existing analytics E2E stays green with the new handlers registered
  </acceptance_criteria>
  <done>All six analytics events are emittable/recordable and llm.called persists a computed cost_usd; the collection layer is complete for 06-02b to aggregate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message.service / llm.service (internal) -> EventEmitter2 -> listener | in-process events; payload originates from WhatsApp/LLM traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | analytics_events payload from new events | low | accept | payload stored as data only, never eval'd or rendered; parameterized inserts via TypeORM (reuse 06-01 recordEvent) |
| T-06-05 | Information Disclosure | cost tracking pricing drift | low | accept | pricing hardcoded constants per RESEARCH §3.3; documented as needing manual update if vendor prices change |
</threat_model>

<verification>
- Six events recordable; cost + percentile math unit-proven
- llm.called persists a computed cost_usd (Groq=0, OpenAI priced)
- Disabled mode remains a proven no-op (06-01 gate reused)
</verification>

<success_criteria>
- The five remaining domain events emit and record when ANALYTICS_ENABLED=true
- Cost and percentile utilities are unit-proven and importable
- Both tasks' automated verifications pass
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by Plan 06-02:
- events on the bus: `conversation.started`, `conversation.resolved`, `conversation.escalated`, `llm.called`, `fallback.triggered`
- function `calculateCost(event)` (analytics-cost.util.ts) + rate constants
- function `percentile(values, q)` (percentile.util.ts)
- llm.service.ts emit site for `llm.called`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-analytics-dashboard/06-02-SUMMARY.md` when done
</output>
