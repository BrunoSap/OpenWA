---
phase: 03-valida-o-e2e-udio-stt
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/audio-stt-cases.e2e-spec.ts
  - test/fixtures/audio/en-clean-expected.json
  - test/fixtures/audio/pt-noisy-expected.json
  - test/support/stt-transcribe.ts
autonomous: true
requirements: [STT-06, STT-07, STT-10]

estimate:
  tokens: 50000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Áudio limpo em inglês é transcrito com acurácia >= 90% — STT-06"
    - "Áudio com ruído de fundo é transcrito e a acurácia degradada é medida e reportada (assert de threshold mais tolerante) — STT-07"
    - "Quando a transcrição falha (timeout ou erro de API), o fluxo cai num fallback determinístico em vez de lançar exceção não tratada — STT-10"
  artifacts:
    - test/audio-stt-cases.e2e-spec.ts
    - test/fixtures/audio/en-clean-expected.json
    - test/fixtures/audio/pt-noisy-expected.json
  key_links:
    - "fixture EN/noisy → transcribeOgg → wordAccuracy → assert por caso"
    - "transcribeWithFallback → em erro/timeout retorna resultado de fallback (não lança)"
    - "resultado de fallback → mensagem determinística ao usuário pedindo reenvio em texto"
---

<objective>
Expandir a cobertura do fluxo STT a partir do tracer PT limpo (Plan 03-01) para os demais casos exigidos: inglês limpo (STT-06), áudio com ruído de fundo (STT-07) e o caminho de fallback quando a transcrição falha (STT-10).

Purpose: Provar que o pipeline STT funciona além do caminho feliz — múltiplos idiomas, degradação de qualidade sob ruído (medida, não assumida) e resiliência a falhas de transcrição.

Output: Suite E2E de casos verde reutilizando o helper e o padrão do tracer; wrapper de fallback determinístico adicionado ao helper e coberto por teste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-valida-o-e2e-udio-stt/03-01-SUMMARY.md
@docs/GUIDES.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fixtures EN limpo e PT com ruído + wrapper de fallback</name>
  <files>test/fixtures/audio/en-clean-expected.json, test/fixtures/audio/pt-noisy-expected.json, test/support/stt-transcribe.ts</files>
  <read_first>
    - test/support/stt-transcribe.ts (helper do Plan 03-01: transcribeOgg, wordAccuracy)
    - test/fixtures/audio/pt-clean-expected.json (schema de fixture estabelecido no Plan 03-01)
    - test/fixtures/audio/README.md (política de skip e origem dos .ogg)
  </read_first>
  <action>
Adicionar dois fixtures novos seguindo exatamente o schema do Plan 03-01:
- `en-clean-expected.json`: `file`, `language` `en`, `durationSec` (~10), `expectedTranscript` (frase clara em inglês), `minAccuracy` `0.9`.
- `pt-noisy-expected.json`: `file`, `language` `pt`, `durationSec` (~10), `expectedTranscript` (mesma classe de frase do PT limpo, mas o .ogg contém ruído de fundo), `minAccuracy` com threshold mais tolerante para ruído (ex.: `0.6`) — o objetivo é MEDIR a degradação, não exigir 90% sob ruído.

Prover os respectivos .ogg em `test/fixtures/audio/` (EN limpo e PT ruidoso ~10s) e citá-los nos JSONs. Se algum .ogg não puder ser incluído no commit, registrar no README e o caso correspondente pula com aviso (mesmo padrão de skip por chave/fixture ausente do Plan 03-01).

Estender `test/support/stt-transcribe.ts` com o wrapper de fallback:
- `transcribeWithFallback(audio: Buffer, opts: { language?: string; model?: string; timeoutMs?: number }): Promise<{ text: string; latencyMs: number; ok: boolean; fallbackReason?: string }>`.
- Envolve `transcribeOgg` num controle de timeout (`AbortController` ou `Promise.race` com `timeoutMs`, default 5000).
- Em timeout ou erro HTTP/rede: NÃO lança; retorna `{ ok: false, text: '', latencyMs, fallbackReason }` onde `fallbackReason` distingue `timeout` de `api_error`.
- Em sucesso: retorna `{ ok: true, text, latencyMs }`.
- Exportar também `buildFallbackReply(fallbackReason: string): string` — retorna a mensagem determinística a enviar ao usuário quando a transcrição falha (pedir reenvio como texto). Manter puro e testável.
  </action>
  <verify>
    <automated>node -e "const j=require('./test/fixtures/audio/en-clean-expected.json'); if(j.language!=='en'||j.minAccuracy<0.9) process.exit(1); const n=require('./test/fixtures/audio/pt-noisy-expected.json'); if(n.language!=='pt'||typeof n.expectedTranscript!=='string') process.exit(1); console.log('fixtures ok')"</automated>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep 'stt-transcribe' | grep -vc '^#'; test "${PIPESTATUS[0]:-0}" = "0" || true</automated>
  </verify>
  <done>Fixtures EN e PT-noisy existem com schema válido; `transcribeWithFallback` e `buildFallbackReply` compilam sem erro de tipo; wrapper de fallback não lança em erro (retorna `ok:false` com `fallbackReason`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Casos E2E — EN limpo + PT com ruído (acurácia medida por caso)</name>
  <files>test/audio-stt-cases.e2e-spec.ts</files>
  <read_first>
    - test/audio-stt-e2e-cycle.e2e-spec.ts (padrão tracer do Plan 03-01: jest.mock('archiver'), setTimeout, skip gracioso, leitura de fixture, asserts de acurácia/latência)
    - test/support/stt-transcribe.ts (transcribeOgg, wordAccuracy)
    - test/fixtures/audio/en-clean-expected.json, test/fixtures/audio/pt-noisy-expected.json
  </read_first>
  <action>
Criar a suite de casos reutilizando o padrão do tracer.

**Setup (idêntico ao tracer):** `jest.mock('archiver', ...)`, `jest.setTimeout(60000)`, skip gracioso quando `GROQ_API_KEY` ausente ou .ogg do caso ausente (por caso, não global — um caso pode pular sem afetar os outros).

**Caso STT-06 (EN limpo):**
- Carregar `en-clean-expected.json` + buffer do .ogg EN.
- `transcribeOgg(buffer, { language: 'en' })`; `wordAccuracy(expectedTranscript, text)` deve ser >= 0.9; logar acurácia e latência.

**Caso STT-07 (PT com ruído):**
- Carregar `pt-noisy-expected.json` + buffer do .ogg ruidoso.
- `transcribeOgg(buffer, { language: 'pt' })`; MEDIR `wordAccuracy` e logar o valor explicitamente (a degradação precisa ser observável); asserir >= `minAccuracy` do fixture (threshold tolerante, ex.: 0.6). O ponto do caso é que o pipeline lida com ruído e a acurácia degradada é reportada, não que atinge 90% sob ruído.

Cada `it` inicia com o guard de skip do caso.
  </action>
  <verify>
    <automated>npm run test:e2e:stt 2>&1 | tail -30</automated>
  </verify>
  <done>Suite `audio-stt-cases.e2e-spec.ts` roda verde: EN limpo transcreve com acurácia >= 0.9; PT ruidoso transcreve e loga a acurácia degradada medida, passando no threshold tolerante; casos pulam individualmente com aviso quando chave/.ogg ausente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Caso E2E de fallback quando a transcrição falha</name>
  <files>test/audio-stt-cases.e2e-spec.ts</files>
  <read_first>
    - test/support/stt-transcribe.ts (transcribeWithFallback, buildFallbackReply criados no Task 1)
  </read_first>
  <action>
Adicionar ao `audio-stt-cases.e2e-spec.ts` o bloco de fallback (STT-10). Este caso NÃO depende de `GROQ_API_KEY` real — ele força a condição de falha:

**Caso STT-10a (timeout):**
- Chamar `transcribeWithFallback(buffer, { timeoutMs: 1 })` (timeout absurdamente curto força timeout) OU apontar para um host inalcançável via opção de teste; asserir que a promessa RESOLVE (não rejeita) com `ok:false` e `fallbackReason === 'timeout'`.

**Caso STT-10b (erro de API):**
- Forçar erro de API (ex.: `GROQ_API_KEY` inválida temporária no escopo do teste, ou buffer vazio que a API rejeita); asserir `ok:false` e `fallbackReason === 'api_error'`.

**Caso STT-10c (mensagem de fallback):**
- Asserir que `buildFallbackReply('timeout')` e `buildFallbackReply('api_error')` retornam string não-vazia orientando o usuário a reenviar em texto — provando que o fluxo tem uma resposta determinística ao usuário quando a transcrição falha.

Estes casos rodam de forma determinística em CI mesmo sem secret configurado.
  </action>
  <verify>
    <automated>npm run test:e2e:stt 2>&1 | grep -iE "STT-10|fallback|pass|fail" | tail -20</automated>
  </verify>
  <done>Os casos de fallback provam que a falha de transcrição (timeout e erro de API) resolve para um resultado de fallback determinístico sem lançar, e que existe uma mensagem de resposta ao usuário; esses casos passam em CI sem necessidade de chave válida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/CI → Groq API | Chave GROQ_API_KEY cruza para serviço externo; via env/secret, nunca literal |
| condição de falha forçada | Testes de fallback manipulam timeout/chave apenas no escopo do teste, sem vazar estado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | falha/timeout de transcrição em produção | high | mitigate | `transcribeWithFallback` garante degradação graciosa (fallback), coberto por STT-10; sem exceção não tratada no caminho de áudio |
| T-03-05 | Information Disclosure | chave inválida temporária no teste de erro | medium | mitigate | Manipulação restrita ao escopo do `it`; nenhuma chave real logada; restaurar env após o caso |
| T-03-06 | Repudiation | acurádia sob ruído não observável | low | mitigate | Acurácia degradada logada explicitamente (STT-07), tornando o resultado auditável |
</threat_model>

<verification>
- `npm run test:e2e:stt` cobre EN limpo, PT ruidoso e os três sub-casos de fallback.
- Casos de fallback (STT-10) passam sem GROQ_API_KEY (determinísticos).
- Acurácia por caso logada (EN e noisy), tornando a degradação observável.
- Nenhuma chave literal nos fixtures ou no spec (grep negativo por `gsk_`).
</verification>

<success_criteria>
- STT-06: EN limpo com acurácia >= 90%.
- STT-07: PT ruidoso transcrito com acurácia degradada medida e reportada.
- STT-10: falha de transcrição (timeout + erro de API) cai em fallback determinístico com mensagem ao usuário, sem lançar.
</success_criteria>

<output>
Create `.planning/phases/03-valida-o-e2e-udio-stt/03-02-SUMMARY.md` when done
</output>
