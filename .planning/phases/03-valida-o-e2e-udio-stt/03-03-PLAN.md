---
phase: 03-valida-o-e2e-udio-stt
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - test/audio-workflow-shape.e2e-spec.ts
  - .github/workflows/audio-stt-e2e.yml
autonomous: true
requirements: [STT-01, STT-02, STT-03, STT-04]

estimate:
  tokens: 45000
  raw_tokens: 34000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "O workflow n8n WhatsApp-Audio-Transcription.json é JSON válido, tem webhook de entrada e nó de transcrição/LLM, e não contém segredos literais — STT-01, STT-03, STT-04"
    - "O workflow referencia a rota de envio de resposta ao WhatsApp e o processamento do evento de transcrição — STT-02, STT-04"
    - "As suites STT rodam automaticamente no CI (GitHub Actions) em PRs que tocam arquivos de áudio/STT"
  artifacts:
    - test/audio-workflow-shape.e2e-spec.ts
    - .github/workflows/audio-stt-e2e.yml
  key_links:
    - "workflow JSON → shape test (JSON válido + webhook + nós + grep negativo de segredos)"
    - "GitHub Actions → npm run test:e2e:stt → suites tracer + casos + fallback"
    - "GROQ_API_KEY via secrets do CI → env do job (skip gracioso quando ausente)"
---

<objective>
Validar a forma (shape) do workflow n8n de áudio existente (`WhatsApp-Audio-Transcription.json`) e integrar as suites STT ao CI/CD, reutilizando o pipeline e os padrões da Fase 2.

Purpose: Garantir que o workflow documentado é estruturalmente válido e livre de segredos literais, e que toda a validação STT roda automaticamente em PRs — fechando os deliverables "workflow importável" e "testes rodam no CI" da fase.

Output: Suite de shape do workflow verde + GitHub Actions workflow dedicado às suites STT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-valida-o-e2e-udio-stt/03-01-SUMMARY.md
@.planning/phases/03-valida-o-e2e-udio-stt/03-02-SUMMARY.md
@WhatsApp-Audio-Transcription.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Teste de shape do workflow n8n de áudio</name>
  <files>test/audio-workflow-shape.e2e-spec.ts</files>
  <read_first>
    - test/intake-workflow-shape.e2e-spec.ts (padrão de shape test: readFileSync + JSON.parse, asserts em name/nodes/connections, webhook presente, httpRequest referenciando rota, grep negativo de segredos literais e x-api-key via $env)
    - WhatsApp-Audio-Transcription.json (workflow real: webhook path 'whatsapp-audio', evento 'message.transcription', nós de código/LLM Groq/envio via /messages/send-text)
  </read_first>
  <action>
Criar a suite de shape do workflow de áudio espelhando `intake-workflow-shape.e2e-spec.ts`.

**Carregamento:** `readFileSync(workflowPath, 'utf8')` do arquivo `WhatsApp-Audio-Transcription.json` no root do repo; `JSON.parse` uma vez em `beforeAll`.

**Asserts de shape:**
- Parseia como JSON válido; `name` é string; `nodes` é array não-vazio; `connections` é objeto não-nulo.
- Tem pelo menos um nó webhook de entrada (`n8n-nodes-base.webhook`).
- Tem um nó que processa a transcrição (o nó de código que valida o evento `message.transcription` e extrai `transcription.text`) — asserir presença via inspeção dos nós de código.
- Tem um nó de LLM (tipo langchain chainLlm / lmChatGroq) provando o encadeamento transcrição → LLM (STT-04).
- Tem um nó httpRequest de envio de resposta cujo `url` referencia a rota de envio ao WhatsApp (`/messages/send-text`).

**Asserts de segurança (grep negativo — guardam T-03-01):**
- O JSON serializado NÃO contém chaves literais: negativar padrões de chave Groq (prefixo de chave secreta da Groq), chave OpenAI (prefixo `sk-`) e Bearer com token literal.
- Toda referência a chave/base URL deve ser via `$env` (ex.: `$env.GROQ_API_KEY`); asserir que a serialização contém referência `$env` para credenciais e não valores embutidos.

IMPORTANTE (disciplina de comentário): não incluir no corpo de asserts negativos o próprio texto literal que o teste está proibindo — construir os padrões via variáveis/regex montadas, não como string proibida escrita por extenso no `<action>`/comentário.
  </action>
  <verify>
    <automated>npx jest --config ./test/jest-e2e.json --testPathPatterns='audio-workflow-shape\.e2e-spec\.ts$' --runInBand 2>&1 | tail -25</automated>
  </verify>
  <done>Suite `audio-workflow-shape.e2e-spec.ts` verde: workflow parseia como JSON válido, tem webhook + nó de transcrição + nó LLM + httpRequest para /messages/send-text, e nenhum segredo literal (só referências $env).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GitHub Actions workflow para as suites STT</name>
  <files>.github/workflows/audio-stt-e2e.yml</files>
  <read_first>
    - .github/workflows/rag-e2e.yml (padrão de CI da Fase 2: triggers por paths, services postgres+redis, setup node, npm ci, env com GROQ_API_KEY via secrets, upload de artefatos, SHAs pinados das actions)
  </read_first>
  <action>
Criar workflow `audio-stt-e2e.yml` espelhando `rag-e2e.yml`, adaptado para STT.

**Triggers (`on`):**
- `pull_request` (branches main, develop) com `paths`: `test/audio-*.e2e-spec.ts`, `test/support/stt-transcribe.ts`, `test/fixtures/audio/**`, `WhatsApp-Audio-Transcription.json`, `.github/workflows/audio-stt-e2e.yml`.
- `push` em main; `workflow_dispatch`.

**Job `audio-stt-e2e`:**
- `runs-on: ubuntu-latest`, `timeout-minutes: 15`, `permissions: contents: read`.
- Reusar os service containers postgres (pgvector/pgvector:pg16) e redis do padrão RAG, pois as suites STT bootam o AppModule (que exige DB/Redis) — manter os mesmos env de banco do rag-e2e.
- Steps: checkout, setup-node (v22, cache npm), `npm ci`.
- Rodar migrations/extensão apenas se necessário para boot (reusar os steps do rag-e2e que criam extensão vector, schema_migrations e rodam `database/scripts/run_migrations_v2.sh`).
- Step principal: `npm run test:e2e:stt` com env: `NODE_ENV=test`, `DATABASE_*` (mesmos do rag-e2e), `REDIS_*`, e `GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}`.
- Documentar (comentário no YAML) que os casos que exigem transcrição real pulam graciosamente quando `GROQ_API_KEY`/fixtures .ogg estão ausentes; os casos de fallback (STT-10) rodam determinísticos sempre.
- Step final: upload de artefatos (test-results/, coverage/) com `if: always()`.
- Pinar todas as actions por SHA (mesmo estilo do rag-e2e.yml).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/audio-stt-e2e.yml','utf8'); const need=['test:e2e:stt','GROQ_API_KEY','pull_request','workflow_dispatch','audio-']; for(const n of need){ if(!y.includes(n)){ console.error('missing',n); process.exit(1);} } console.log('ci workflow ok')"</automated>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/audio-stt-e2e.yml')); print('yaml valid')"</automated>
  </verify>
  <done>`.github/workflows/audio-stt-e2e.yml` é YAML válido, dispara em PR/push/dispatch nos paths de áudio, roda `npm run test:e2e:stt` com `GROQ_API_KEY` via secrets e faz upload de artefatos; actions pinadas por SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow JSON → repositório | Workflow versionado não pode conter credenciais literais |
| CI → Groq API | GROQ_API_KEY injetada via `secrets` do GitHub, nunca em texto no YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | segredos literais em WhatsApp-Audio-Transcription.json | high | mitigate | Shape test faz grep negativo de padrões de chave (Groq/OpenAI/Bearer) e exige referências $env |
| T-03-07 | Elevation of Privilege | permissões amplas do job CI | medium | mitigate | `permissions: contents: read`; actions pinadas por SHA (supply-chain) |
| T-03-08 | Tampering | actions de terceiros no CI | medium | mitigate | Todas as actions pinadas por SHA de commit, espelhando rag-e2e.yml |
</threat_model>

<verification>
- Suite de shape do workflow verde e sem segredos literais.
- `.github/workflows/audio-stt-e2e.yml` é YAML válido, roda `test:e2e:stt`, injeta GROQ_API_KEY via secrets, pina actions por SHA.
- CI dispara nos paths corretos de áudio/STT.
</verification>

<success_criteria>
- Workflow n8n de áudio validado estruturalmente (webhook + transcrição + LLM + envio) e livre de segredos (STT-01/03/04, guarda T-03-01).
- Suites STT (tracer + casos + fallback) rodam automaticamente no CI em PRs de áudio.
- Padrões da Fase 2 (services, pinning, upload de artefatos) reutilizados.
</success_criteria>

<output>
Create `.planning/phases/03-valida-o-e2e-udio-stt/03-03-SUMMARY.md` when done
</output>
