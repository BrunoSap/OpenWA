---
phase: 01-bot-de-intake-e2e
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Whatsapp-Intake-Bot.json
  - test/intake-workflow-shape.e2e-spec.ts
autonomous: true
requirements: [INTAKE-05]
requirements_addressed: [INTAKE-05]

estimate:
  tokens: 45000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe um workflow n8n (Whatsapp-Intake-Bot.json) que orquestra o fluxo conversacional de intake (INTAKE-05)"
    - "O workflow e importavel no n8n (JSON valido com nodes + connections) e configuravel (webhook de entrada + chamada HTTP para a API de intake do OpenWA)"
  artifacts:
    - Whatsapp-Intake-Bot.json
    - test/intake-workflow-shape.e2e-spec.ts
  key_links:
    - "Node httpRequest do workflow -> POST /api/sessions/{sessionId}/intake/messages (rota do Plan 01) com header de API key"
    - "Node webhook do workflow recebe a mensagem WhatsApp do OpenWA (ingress) e passa chatId+text ao node de ingestao"
---

<objective>
Criar o workflow n8n que orquestra o Bot de Intake ponta-a-ponta no n8n, reusando as rotas HTTP do modulo intake (Plans 01/02) como backend de estado.

Purpose: O ROADMAP exige um workflow n8n importavel (INTAKE-05). Em vez de reimplementar a maquina de estado dentro do n8n, o workflow delega a coleta estruturada a API `/intake/messages` do OpenWA (fonte unica da verdade), e devolve a `reply` ao cliente via OpenWA. Isso mantem a logica testavel no backend e o workflow fino/orquestrador.

Output: `Whatsapp-Intake-Bot.json` importavel no n8n + um teste de forma (shape) que garante que o JSON e valido e aponta para as rotas corretas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bot-de-intake-e2e/01-01-SUMMARY.md

# Padrao de workflow existente (fonte de estrutura):
@Whatsapp-Unified-Bot-FIXED.json
@docs/WORKFLOWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workflow n8n Whatsapp-Intake-Bot.json orquestrando ingest -> reply</name>
  <read_first>
    - Whatsapp-Unified-Bot-FIXED.json (estrutura: name, nodes[], connections{}, settings; tipos de node n8n-nodes-base.webhook / .code / .if / .httpRequest)
    - docs/WORKFLOWS.md (convencao de import/config de workflows do projeto)
    - Whatsapp-Intake-Bot.json (arquivo a criar)
  </read_first>
  <action>
    Criar `Whatsapp-Intake-Bot.json` no root do repo (mesmo local dos outros workflows), com as chaves top-level `name`, `nodes`, `connections`, `active` (false), `settings`, `meta`, `tags` — mesma forma de Whatsapp-Unified-Bot-FIXED.json.
    Nodes (minimo):
    1. `n8n-nodes-base.webhook` — recebe o evento de mensagem WhatsApp do OpenWA (path ex.: `intake`). Extrai `chatId` e `text` do payload de entrada.
    2. `n8n-nodes-base.httpRequest` — POST para `={{$env.OPENWA_BASE_URL}}/api/sessions/{{$json.sessionId}}/intake/messages` com body JSON contendo os campos `chatId` e `text` mapeados do webhook, e header `x-api-key` referenciando `={{$env.OPENWA_API_KEY}}` (a rota exige @RequireRole OPERATOR do Plan 01). Usar variaveis de ambiente n8n para base url e api key (configuravel, sem segredo literal).
    3. `n8n-nodes-base.httpRequest` (ou node de envio OpenWA) — envia a `reply` retornada pelo passo anterior de volta ao chatId via API de envio de mensagem do OpenWA.
    Definir `connections` ligando webhook -> ingest httpRequest -> send reply. Nao embutir chaves de API ou tokens literais no JSON — apenas referencias a `$env`. Nome do workflow: "WhatsApp Intake Bot".
  </action>
  <verify>
    <automated>node -e "const w=require('./Whatsapp-Intake-Bot.json'); if(!Array.isArray(w.nodes)||!w.connections||!w.name){throw new Error('shape invalido')}; const s=JSON.stringify(w); if(!s.includes('/intake/messages')){throw new Error('rota de ingest ausente')}; console.log('workflow OK', w.nodes.length, 'nodes')"</automated>
  </verify>
  <done>Whatsapp-Intake-Bot.json e JSON valido, importavel no n8n, com node webhook de entrada, node httpRequest que faz POST para /api/sessions/{sessionId}/intake/messages via $env, e node que reenvia a reply. Nenhum segredo literal no JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de shape garante workflow valido e rotas corretas</name>
  <read_first>
    - Whatsapp-Intake-Bot.json (criado na Task 1)
    - test/integration-fabric.e2e-spec.ts (padrao de leitura de fixture JSON com readFileSync/join e describe/it/expect)
    - test/intake-workflow-shape.e2e-spec.ts (arquivo a criar)
  </read_first>
  <action>
    Criar `test/intake-workflow-shape.e2e-spec.ts` que carrega `Whatsapp-Intake-Bot.json` (readFileSync + JSON.parse) e faz asserts com Jest/expect:
    - JSON parseia sem erro e tem `name`, `nodes` (array nao vazio), `connections` (objeto).
    - Existe pelo menos um node de tipo `n8n-nodes-base.webhook` (entrada).
    - Existe um node httpRequest cuja URL/parametros contem a substring `/intake/messages` (delega ao backend).
    - O JSON serializado NAO contem chaves de API literais: asserir que nao ha string casando `/sk-[A-Za-z0-9]/` nem `x-api-key` seguido de valor literal — apenas referencias `$env`. Para checar segredos, negar-grep a ausencia de padroes de chave real; a string permitida `$env` pode aparecer.
    Manter os literais de match do negative-grep FORA de qualquer comentario que os invalide.
  </action>
  <verify>
    <automated>npm run test:e2e -- intake-workflow-shape</automated>
  </verify>
  <done>test/intake-workflow-shape.e2e-spec.ts passa: workflow e JSON valido, tem node webhook, tem httpRequest apontando para /intake/messages, e nao contem segredos literais (usa $env).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n webhook -> OpenWA API | o workflow chama a API de intake com uma API key; a key vem de $env, nunca do JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | segredo hardcoded no JSON do workflow | high | mitigate | somente referencias $env; teste de shape nega-grep segredos literais |
| T-03-02 | Spoofing | webhook n8n aberto | medium | accept | autenticacao do webhook n8n e config de deploy (fora do escopo do JSON); documentado no Plan 04 |
</threat_model>

<verification>
- `node -e` valida shape do JSON e presenca da rota /intake/messages
- `npm run test:e2e -- intake-workflow-shape` passa
- Nenhum segredo literal no workflow (apenas $env)
</verification>

<success_criteria>
Workflow n8n importavel e configuravel existe, delega a coleta a API de intake do OpenWA e reenvia a reply, sem segredos embutidos.
</success_criteria>

<output>
Create `.planning/phases/01-bot-de-intake-e2e/01-03-SUMMARY.md` when done
</output>
