---
phase: 01-bot-de-intake-e2e
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - test/intake-e2e-cycle.e2e-spec.ts
  - docs/GUIDES.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [INTAKE-07]
requirements_addressed: [INTAKE-07]

estimate:
  tokens: 55000
  raw_tokens: 29000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um teste E2E automatizado percorre o ciclo completo: mensagem WhatsApp -> coleta conversacional -> lead persistido em banco -> export do lead qualificado (INTAKE-07)"
    - "GUIDES.md documenta o Bot de Intake com exemplo de uso e o aviso de status desatualizado e removido/atualizado"
  artifacts:
    - test/intake-e2e-cycle.e2e-spec.ts
  key_links:
    - "Teste E2E exercita as rotas dos Plans 01/02 e valida transicao in_progress -> completed -> export"
    - "GUIDES.md e ARCHITECTURE.md refletem a implementacao real (nao mais 'implementacao pendente')"
---

<objective>
Fechar a fase com o teste E2E do ciclo completo de intake e atualizar a documentacao para refletir a implementacao real.

Purpose: INTAKE-07 exige um teste E2E que prove o ciclo inteiro (WhatsApp -> coleta -> banco -> export). O tracer (Plan 01) provou uma fatia; aqui validamos a jornada conversacional completa de ponta a ponta. E a documentacao (ARCHITECTURE.md L129-178 marcava "IMPLEMENTACAO PENDENTE") deve ser corrigida.

Output: Suite E2E do ciclo completo verde + docs atualizados com exemplo de uso e status correto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bot-de-intake-e2e/01-01-SUMMARY.md
@.planning/phases/01-bot-de-intake-e2e/01-02-SUMMARY.md
@.planning/phases/01-bot-de-intake-e2e/01-03-SUMMARY.md

# Padroes:
@test/integration-fabric.e2e-spec.ts
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste E2E do ciclo completo de intake</name>
  <read_first>
    - test/integration-fabric.e2e-spec.ts (bootstrap E2E, applyGlobalValidation, setGlobalPrefix('api'), supertest, getRepositoryToken(..., 'data'), setup de API key)
    - src/modules/intake/intake.controller.ts (rotas: POST messages, GET leads/:chatId, POST leads/:chatId/export)
    - src/modules/intake/intake-flow.ts (ordem dos steps e perguntas esperadas)
    - test/intake-e2e-cycle.e2e-spec.ts (arquivo a criar)
  </read_first>
  <behavior>
    - Test 1: enviar sequencialmente 5 mensagens (nome, telefone, email, demanda, urgencia) via POST /api/sessions/:sid/intake/messages e cada resposta contem a proxima pergunta esperada do fluxo
    - Test 2: apos a 5a mensagem, GET /api/sessions/:sid/intake/leads/:chatId retorna intake_status='completed' com os 5 campos preenchidos
    - Test 3: POST /api/sessions/:sid/intake/leads/:chatId/export para uma url de teste (servidor http local capturando o POST) recebe o payload do lead qualificado com fullName/email/urgencyLevel corretos
    - Test 4: o lead persiste na conexao 'data' (verificado via Repository<IntakeLead> count === 1 para o chatId)
  </behavior>
  <action>
    Criar `test/intake-e2e-cycle.e2e-spec.ts` seguindo o harness de test/integration-fabric.e2e-spec.ts: bootar AppModule, `applyGlobalValidation`, `app.setGlobalPrefix('api')`, obter `Repository<IntakeLead>` via `getRepositoryToken(IntakeLead, 'data')`, autenticar com o header de API key OPERATOR do harness.
    Para o alvo de export, subir um servidor http efemero (node:http `createServer`) em `127.0.0.1` porta 0, capturar o body recebido, e passar sua url ao POST /export; asserir o payload capturado. Fechar o servidor no afterAll.
    Percorrer o ciclo completo: 5 POSTs de mensagem em ordem, checar as replies contra as perguntas de intake-flow.ts, GET final valida 'completed', export valida entrega. Limpar o lead do repositorio no beforeEach/afterEach para isolar o teste.
  </action>
  <verify>
    <automated>npm run test:e2e -- intake-e2e-cycle</automated>
  </verify>
  <done>test/intake-e2e-cycle.e2e-spec.ts passa cobrindo WhatsApp(msg) -> coleta 5 campos -> lead 'completed' persistido -> export recebido pelo servidor de teste. INTAKE-07 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar GUIDES.md e ARCHITECTURE.md para refletir a implementacao</name>
  <read_first>
    - docs/ARCHITECTURE.md (secao "Bot de Intake" L129-178, incluindo o bloco de aviso de status)
    - docs/GUIDES.md (secao de guias de implementacao / workflows)
    - src/modules/intake/intake.controller.ts (rotas reais para documentar)
    - Whatsapp-Intake-Bot.json (workflow a referenciar)
  </read_first>
  <action>
    Em `docs/ARCHITECTURE.md`, substituir o bloco de aviso "STATUS: SCHEMA CRIADO, IMPLEMENTACAO PENDENTE" da secao Bot de Intake por um status que reflete a implementacao (controller/service/workflow/testes E2E entregues na Phase 1), citando o modulo `src/modules/intake` e o workflow `Whatsapp-Intake-Bot.json`.
    Em `docs/GUIDES.md`, adicionar uma subsecao "Bot de Intake" com: (a) as rotas REST reais (POST /sessions/:sessionId/intake/messages, GET /sessions/:sessionId/intake/leads/:chatId, POST /sessions/:sessionId/intake/leads/:chatId/export) com exemplo de request/response; (b) como importar e configurar Whatsapp-Intake-Bot.json no n8n (variaveis $env OPENWA_BASE_URL e OPENWA_API_KEY); (c) a ordem do fluxo conversacional (nome -> telefone -> email -> demanda -> urgencia). Texto em pt-BR, identificadores tecnicos em ingles.
    Nao inventar rotas ou campos que nao existem no codigo — usar exatamente os nomes das rotas/DTOs implementados nos Plans 01/02.
  </action>
  <verify>
    <automated>grep -q "IMPLEMENTACAO PENDENTE\|IMPLEMENTAÇÃO PENDENTE" docs/ARCHITECTURE.md && echo "AINDA PENDENTE — corrigir" && exit 1 || echo "aviso de pendencia removido"</automated>
    <automated>grep -q "intake/messages" docs/GUIDES.md && echo "rotas documentadas em GUIDES"</automated>
  </verify>
  <done>ARCHITECTURE.md nao contem mais o aviso "IMPLEMENTACAO PENDENTE" para o Bot de Intake; GUIDES.md documenta as rotas reais, o import do workflow e a ordem do fluxo conversacional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste E2E -> servidor http efemero | payload de export capturado em 127.0.0.1 durante o teste (sem exposicao externa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | servidor de captura no teste | low | accept | bind em 127.0.0.1 porta efemera, fechado no afterAll — apenas ambiente de teste |
</threat_model>

<verification>
- `npm run test:e2e -- intake-e2e-cycle` passa (ciclo completo)
- `docs/ARCHITECTURE.md` sem aviso de pendencia; `docs/GUIDES.md` com rotas de intake
</verification>

<success_criteria>
O ciclo completo do Bot de Intake e provado por um teste E2E automatizado e a documentacao reflete a implementacao real.
</success_criteria>

<output>
Create `.planning/phases/01-bot-de-intake-e2e/01-04-SUMMARY.md` when done
</output>
