---
phase: 01-bot-de-intake-e2e
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/modules/intake/intake-flow.ts
  - src/modules/intake/intake-flow.spec.ts
  - src/modules/intake/intake.service.ts
  - src/modules/intake/intake.controller.ts
  - src/modules/intake/dto/index.ts
autonomous: true
requirements: [INTAKE-02, INTAKE-04]
requirements_addressed: [INTAKE-02, INTAKE-04]

estimate:
  tokens: 65000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O bot coleta dados estruturados via fluxo conversacional passo-a-passo: nome, telefone, email, demanda, urgencia (INTAKE-02)"
    - "Cada mensagem avanca o estado do lead e retorna a proxima pergunta; ao completar, intake_status vira 'completed'"
    - "Um lead qualificado (completed) e exportavel via API REST e via webhook (INTAKE-04)"
  artifacts:
    - src/modules/intake/intake-flow.ts
    - src/modules/intake/intake-flow.spec.ts
  key_links:
    - "IntakeService.ingestMessage delega ao motor de fluxo (intake-flow.ts) para decidir proxima pergunta e atualizar case_data"
    - "IntakeController expoe POST /intake/:id/export -> IntakeService.export -> WebhookService/HTTP + payload JSON"
---

<objective>
Expandir o tracer (Plan 01) para um fluxo conversacional estruturado real e uma saida de lead qualificado.

Purpose: A ingestao do tracer so acumula mensagens. Esta expansao entrega a coleta estruturada (nome -> telefone -> email -> demanda -> urgencia) que qualifica o lead, e a exportacao do lead completo para consumo externo (CRM via webhook ou leitura API).

Output: Motor de fluxo determinístico (pure function, testavel isoladamente), ingestMessage que avanca o estado e devolve a proxima pergunta, e rota de export do lead qualificado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bot-de-intake-e2e/01-01-SUMMARY.md

# Padroes:
@src/modules/intake/intake.service.ts
@src/modules/intake/intake.controller.ts
@src/modules/webhook/webhook-delivery.service.ts
@src/modules/webhook/dto/webhook.dto.ts
@database/migrations/003_create_schema_intake_staging.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor de fluxo conversacional determinístico (pure function)</name>
  <read_first>
    - database/migrations/003_create_schema_intake_staging.sql (campos coletados: full_name, phone, email, case_type, urgency_level)
    - src/modules/intake/entities/intake-lead.entity.ts (estrutura do lead e case_data)
    - src/modules/intake/intake-flow.ts (arquivo a criar)
    - src/modules/intake/intake-flow.spec.ts (arquivo a criar)
  </read_first>
  <behavior>
    - Test 1: estado inicial (nenhum campo) -> proxima pergunta pede o nome; step = 'collect_name'
    - Test 2: recebendo texto no step collect_name -> grava fullName, avanca para 'collect_phone' e pergunta o telefone
    - Test 3: sequencia completa name -> phone -> email -> demanda -> urgencia produz status 'completed' e nenhuma proxima pergunta
    - Test 4: input de urgencia invalido (fora de normal/high/critical) mantem o step e repete a pergunta de urgencia
  </behavior>
  <action>
    Criar `intake-flow.ts` exportando um tipo `IntakeStep = 'collect_name' | 'collect_phone' | 'collect_email' | 'collect_demand' | 'collect_urgency' | 'completed'` e uma pure function `advanceIntake(state: { fullName?: string; phone?: string; email?: string; caseType?: string; urgencyLevel?: string }, message: string): { nextState: typeof state; step: IntakeStep; reply: string; completed: boolean }`.
    Regras deterministicas (sem I/O, sem repositorio — so logica pura para ser 100% testavel):
    - Determinar o step atual pelo primeiro campo ainda vazio na ordem: fullName, phone, email, caseType, urgencyLevel.
    - Ao receber `message`, gravar no campo do step atual, exceto para urgencyLevel: normalizar `message` e aceitar apenas 'normal' | 'high' | 'critical' (aceitar sinonimos pt: 'baixa'->normal, 'media'->high? NAO — mapear 'normal'/'alta'->'high'/'critica'->'critical' explicitamente: 'baixa'|'normal' -> normal, 'alta'|'high' -> high, 'critica'|'urgente'|'critical' -> critical). Input nao reconhecido: NAO grava, repete a pergunta de urgencia.
    - `reply` e a pergunta pt-BR do proximo step (ex.: "Qual e o seu nome completo?"). Quando todos preenchidos, step='completed', completed=true, reply = mensagem de confirmacao com resumo dos dados.
    Nao mutar o `state` de entrada — retornar `nextState` novo.
  </action>
  <verify>
    <automated>npx jest src/modules/intake/intake-flow.spec.ts</automated>
  </verify>
  <done>intake-flow.spec.ts passa: advanceIntake e pura, determina step pelo primeiro campo vazio, valida urgencyLevel contra normal/high/critical, e retorna completed=true so quando os 5 campos estao preenchidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntakeService usa o motor de fluxo e marca completed</name>
  <read_first>
    - src/modules/intake/intake.service.ts (ingestMessage do tracer — a ser estendido)
    - src/modules/intake/intake-flow.ts (advanceIntake da Task 1)
    - src/modules/intake/entities/intake-lead.entity.ts
  </read_first>
  <behavior>
    - Test 1: ingestMessage num lead novo grava a 1a resposta como fullName e persiste, retornando reply pedindo telefone
    - Test 2: apos coletar os 5 campos, intake_status do lead vira 'completed' e intake_completed_at e setado
    - Test 3: reply retornado a cada passo corresponde a proxima pergunta do fluxo
  </behavior>
  <action>
    Refatorar `IntakeService.ingestMessage` para: carregar/criar o lead por chatId (upsert do tracer preservado); montar o `state` a partir dos campos do lead (fullName, phone, email, caseType, urgencyLevel); chamar `advanceIntake(state, text)`; aplicar `nextState` de volta no lead (fullName/phone/email/caseType/urgencyLevel); se `completed`, setar `intakeStatus = 'completed'` e `caseData.completedAt = new Date().toISOString()` (a coluna intake_completed_at existe no schema Postgres; para a entidade cross-dialect, adicionar campo `intakeCompletedAt` via @Column({ name: 'intake_completed_at', type: dateColumnType(), nullable: true }) na entidade e setar aqui); salvar. Alterar o tipo de retorno de ingestMessage para `{ lead: IntakeLead; reply: string; step: IntakeStep; completed: boolean }`.
    Preservar a idempotencia por chat_id. NAO acumular todas as mensagens em case_data.messages de forma ilimitada — manter apenas as ultimas 50 entradas (mitiga T-01-03).
  </action>
  <verify>
    <automated>npx jest src/modules/intake/intake.service.spec.ts 2>/dev/null || npm run test:e2e -- intake-tracer</automated>
    <automated>grep -q "advanceIntake" src/modules/intake/intake.service.ts && echo "flow wired into service"</automated>
  </verify>
  <done>ingestMessage delega ao advanceIntake, persiste os campos coletados, e ao completar os 5 campos marca intake_status='completed' com intake_completed_at preenchido. Tracer E2E continua verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exportacao do lead qualificado (API REST + webhook)</name>
  <read_first>
    - src/modules/webhook/webhook-delivery.service.ts (padrao de POST HTTP de saida / entrega)
    - src/modules/intake/intake.controller.ts (a estender)
    - src/modules/intake/intake.service.ts
    - src/modules/intake/dto/index.ts
  </read_first>
  <behavior>
    - Test 1 (E2E): GET /api/sessions/:sid/intake/leads/:chatId retorna o lead completo em JSON (export via API)
    - Test 2 (E2E): POST /api/sessions/:sid/intake/leads/:chatId/export com body { url } dispara um POST para a url com o payload do lead e retorna 202/200 com status de entrega
    - Test 3: export de lead ainda 'in_progress' retorna 409 (nao exportar lead incompleto)
  </behavior>
  <action>
    Adicionar `ExportIntakeDto` em dto/index.ts com `@IsUrl() url: string` (opcional: `@IsOptional() @IsObject() headers?: Record<string,string>`).
    Em `IntakeService`, adicionar `async export(chatId: string, target: { url: string; headers?: Record<string,string> }): Promise<{ delivered: boolean; status?: number }>`: carregar o lead por chatId (NotFoundException se ausente); se `intakeStatus !== 'completed'`, lancar `ConflictException('Lead intake not completed')`; montar payload JSON `{ id, chatId, fullName, phone, email, caseType, urgencyLevel, caseData, intakeStatus, intakeCompletedAt }`; fazer POST para `target.url` reusando o cliente HTTP/padrao de entrega de webhook-delivery.service.ts (nao criar cliente novo se houver um util reutilizavel). Retornar delivered/status.
    Em `IntakeController`, adicionar `@Post('leads/:chatId/export') @RequireRole(ApiKeyRole.OPERATOR)` chamando `intakeService.export(chatId, dto)`. Manter a rota GET de leitura do lead (export via API ja coberta pelo tracer GET).
  </action>
  <verify>
    <automated>npm run test:e2e -- intake-tracer</automated>
    <automated>grep -q "ConflictException" src/modules/intake/intake.service.ts && echo "incomplete-lead guard present"</automated>
  </verify>
  <done>Lead 'completed' e exportavel via GET (JSON) e via POST /export (webhook para url alvo); export de lead incompleto retorna 409. INTAKE-04 satisfeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntakeController -> url de export externa | payload de lead (dados pessoais) sai para uma url fornecida pelo chamador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | POST /export url arbitraria | high | mitigate | ExportIntakeDto @IsUrl; @RequireRole(OPERATOR); export so de lead 'completed' |
| T-02-02 | Tampering | urgencyLevel fora do dominio | medium | mitigate | advanceIntake valida contra normal/high/critical, ignora input invalido |
| T-02-03 | Denial of Service | case_data.messages ilimitado | low | mitigate | limitar a 50 entradas em ingestMessage |
</threat_model>

<verification>
- `npx jest src/modules/intake/intake-flow.spec.ts` passa
- `npm run test:e2e -- intake-tracer` continua verde apos refactor
- advanceIntake referenciado em intake.service.ts; ConflictException presente
</verification>

<success_criteria>
Uma conversa passo-a-passo coleta nome/telefone/email/demanda/urgencia, marca o lead como completed, e o lead qualificado e exportavel via API e webhook — export de incompleto e bloqueado.
</success_criteria>

<output>
Create `.planning/phases/01-bot-de-intake-e2e/01-02-SUMMARY.md` when done
</output>
