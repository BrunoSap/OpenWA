---
phase: 01-bot-de-intake-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/intake/entities/intake-lead.entity.ts
  - src/modules/intake/intake.service.ts
  - src/modules/intake/intake.controller.ts
  - src/modules/intake/intake.module.ts
  - src/modules/intake/dto/index.ts
  - src/app.module.ts
  - src/database/data-source.ts
  - test/intake-tracer.e2e-spec.ts
autonomous: true
requirements: [INTAKE-01, INTAKE-03, INTAKE-06]
requirements_addressed: [INTAKE-01, INTAKE-03, INTAKE-06]

estimate:
  tokens: 70000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uma mensagem WhatsApp de entrada cria (ou reusa) uma intake session persistida por chat_id (INTAKE-01)"
    - "Dados coletados são gravados na tabela de leads com timestamps e status (INTAKE-03)"
    - "IntakeService e IntakeController existem e estão registrados no AppModule (INTAKE-06)"
    - "Um teste E2E percorre a fatia: POST mensagem -> lead persistido -> leitura retorna o lead"
  artifacts:
    - src/modules/intake/entities/intake-lead.entity.ts
    - src/modules/intake/intake.service.ts
    - src/modules/intake/intake.controller.ts
    - src/modules/intake/intake.module.ts
    - test/intake-tracer.e2e-spec.ts
  key_links:
    - "IntakeModule registrado em AppModule imports[] -> DI resolve IntakeController"
    - "Glob src/modules/intake/**/*.entity em app.module.ts (conexao 'data') E em src/database/data-source.ts dataEntities -> entidade mapeada nos dois dialetos"
    - "IntakeService injeta Repository<IntakeLead> via getRepositoryToken(IntakeLead, 'data')"
---

<objective>
Tracer end-to-end do Bot de Intake: a fatia mais fina que atravessa todas as camadas que a fase vai tocar (HTTP controller -> service -> entidade TypeORM na conexao 'data' -> persistencia -> leitura), provada por um teste E2E real.

Purpose: Provar a arquitetura de intake (entidade cross-dialect, registro do modulo, wiring do repositorio) em um unico caminho happy antes de expandir o fluxo conversacional e o workflow n8n. Se o registro da entidade ou o wiring do modulo estiver errado, isto falha depois de um commit — nao depois de dez.

Output: Modulo `intake` funcional com uma rota de ingestao que cria/atualiza um lead por chat_id, uma rota de leitura, entidade persistida em SQLite (dev/teste) e Postgres (prod), e teste E2E cobrindo o caminho.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Padroes obrigatorios a seguir (source of truth):
@src/modules/integration/entities/conversation-mapping.entity.ts
@src/common/utils/column-types.ts
@src/modules/integration/integration.module.ts
@src/modules/contact/contact.service.ts
@src/modules/webhook/webhook.controller.ts
@src/modules/auth/decorators/auth.decorators.ts
@database/migrations/003_create_schema_intake_staging.sql
@test/integration-fabric.e2e-spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Entidade IntakeLead cross-dialect + registro nas duas conexoes 'data'</name>
  <read_first>
    - src/modules/integration/entities/conversation-mapping.entity.ts (padrao de entidade da conexao 'data': @Entity, @Column, jsonColumnType)
    - src/common/utils/column-types.ts (jsonColumnType/dateColumnType — DATA CONNECTION ONLY)
    - database/migrations/003_create_schema_intake_staging.sql (colunas fonte-da-verdade da tabela intake_staging.leads)
    - src/app.module.ts (bloco TypeOrmModule.forRootAsync name 'data', array entities[])
    - src/database/data-source.ts (const dataEntities)
    - src/modules/intake/entities/intake-lead.entity.ts (arquivo a criar)
  </read_first>
  <behavior>
    - Test 1: entidade IntakeLead compila e o AppModule inicializa (SQLite) sem erro de "No metadata for IntakeLead"
    - Test 2: salvar um IntakeLead com chat_id gera id numerico e persiste chat_id, case_type, intake_status='in_progress', case_data (JSON) e intake_started_at
  </behavior>
  <action>
    Criar classe `IntakeLead` decorada com `@Entity('intake_leads')` (nome de tabela flat, SEM namespace de schema — SQLite nao suporta schema Postgres; a migration 003 e o caminho Postgres de producao, a entidade e o caminho cross-dialect da conexao 'data').
    Colunas espelhando os campos nucleares de `intake_staging.leads` da migration 003:
    - `id` via `@PrimaryGeneratedColumn()` (SERIAL/INTEGER numerico, igual ao schema — NAO usar uuid).
    - `chatId` (`@Column({ name: 'chat_id' })`), unico via `@Index('UQ_intake_leads_chat_id', ['chatId'], { unique: true })`.
    - `phone`, `cpf`, `fullName` (`@Column({ name: 'full_name', nullable: true })`), `email` — todos `nullable: true`.
    - `caseType` (`@Column({ name: 'case_type' })`), `urgencyLevel` (`@Column({ name: 'urgency_level', default: 'normal' })`).
    - `caseData` (`@Column({ name: 'case_data', type: jsonColumnType() })`) tipado `Record<string, unknown>`.
    - `intakeStatus` (`@Column({ name: 'intake_status', default: 'in_progress' })`).
    - `intakeStartedAt` via `@CreateDateColumn({ name: 'intake_started_at' })` e `updatedAt` via `@UpdateDateColumn({ name: 'updated_at' })`.
    Usar `jsonColumnType()` de src/common/utils/column-types.ts para o campo JSON (NAO hardcode 'jsonb', que quebra SQLite). Nao adicionar CHECK constraints na entidade — validacao de dominio fica no service/DTO.
    Registrar o glob da entidade nas DUAS listas de entities da conexao 'data': em `src/app.module.ts` adicionar a linha `__dirname + '/modules/intake/**/*.entity{.ts,.js}',` ao array `entities` do `baseConfig` da conexao name 'data' (o mesmo array que ja lista session/webhook/integration); em `src/database/data-source.ts` adicionar `sourceGlob('..', 'modules', 'intake', '**', '*.entity{.ts,.js}'),` ao array `dataEntities`. Ambos sao obrigatorios: app.module.ts para runtime, data-source.ts para o CLI de migration.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "intake" || echo "no intake type errors"</automated>
    <automated>grep -c "modules/intake/\*\*/\*.entity" src/app.module.ts | grep -qv '^0$' && echo "app.module glob OK"</automated>
    <automated>grep -c "'modules', 'intake'" src/database/data-source.ts | grep -qv '^0$' && echo "data-source glob OK"</automated>
  </verify>
  <done>IntakeLead existe, usa jsonColumnType() para case_data, tem @Index unico em chat_id, e o glob da entidade aparece tanto em src/app.module.ts (conexao 'data') quanto em src/database/data-source.ts dataEntities.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: IntakeService + IntakeModule (create/upsert por chat_id, read, wiring do repositorio)</name>
  <read_first>
    - src/modules/contact/contact.service.ts (padrao @Injectable, createLogger, NotFoundException)
    - src/modules/integration/integration.module.ts (padrao TypeOrmModule.forFeature([...], 'data') + providers + controllers)
    - src/modules/intake/entities/intake-lead.entity.ts (entidade criada na Task 1)
    - src/modules/intake/intake.service.ts (arquivo a criar)
    - src/modules/intake/intake.module.ts (arquivo a criar)
  </read_first>
  <behavior>
    - Test 1: ingestMessage({ sessionId, chatId, text }) com chat_id novo cria um IntakeLead com intake_status='in_progress' e retorna o lead
    - Test 2: ingestMessage com o mesmo chat_id NAO cria duplicado — reusa o lead existente (upsert por chat_id)
    - Test 3: getByChatId retorna o lead persistido; getByChatId de chat_id inexistente lanca NotFoundException
  </behavior>
  <action>
    Criar `IntakeService` como `@Injectable()` com `private readonly logger = createLogger('IntakeService')`. Injetar o repositorio via construtor com `@InjectRepository(IntakeLead, 'data') private readonly leads: Repository<IntakeLead>` (a conexao nomeada 'data' e obrigatoria — sem ela o token nao resolve).
    Metodos:
    - `async ingestMessage(input: { sessionId: string; chatId: string; text: string }): Promise<IntakeLead>` — busca lead por `chatId`; se nao existe, cria com `caseType: 'unknown'`, `caseData: { messages: [input.text] }`, `intakeStatus: 'in_progress'` e salva; se existe, faz push de `input.text` em `caseData.messages` e salva (upsert idempotente por chat_id). Retorna o lead.
    - `async getByChatId(chatId: string): Promise<IntakeLead>` — busca por chatId; se null, lanca `NotFoundException(\`Intake lead for chat ${chatId} not found\`)`.
    - `async findById(id: number): Promise<IntakeLead>` — busca por id; NotFoundException se null.
    Criar `IntakeModule` com `@Module`: `imports: [TypeOrmModule.forFeature([IntakeLead], 'data')]`, `controllers: [IntakeController]`, `providers: [IntakeService]`, `exports: [IntakeService]`. Registrar `IntakeModule` no array `imports` do `@Module` de `src/app.module.ts` (adicionar `import { IntakeModule }` no topo junto aos outros imports de modulo, e `IntakeModule,` na lista de imports do decorator).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "intake\.(service|module)" || echo "no service/module type errors"</automated>
    <automated>grep -q "IntakeModule" src/app.module.ts && echo "IntakeModule registered in app.module"</automated>
  </verify>
  <done>IntakeService injeta Repository<IntakeLead> na conexao 'data', ingestMessage faz upsert idempotente por chat_id, getByChatId lanca NotFoundException para chat desconhecido, e IntakeModule esta no imports[] do AppModule.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: IntakeController (ingest + read) e teste E2E do caminho end-to-end</name>
  <read_first>
    - src/modules/webhook/webhook.controller.ts (padrao @Controller, @Post, @RequireRole, @ApiTags, @Param, @Body)
    - src/modules/auth/decorators/auth.decorators.ts (RequireRole, ApiKeyRole)
    - src/modules/intake/intake.service.ts (service criado na Task 2)
    - test/integration-fabric.e2e-spec.ts (padrao de bootstrap E2E: Test.createTestingModule, applyGlobalValidation, supertest, getRepositoryToken com 'data')
    - src/modules/intake/intake.controller.ts (arquivo a criar)
    - src/modules/intake/dto/index.ts (arquivo a criar)
    - test/intake-tracer.e2e-spec.ts (arquivo a criar)
  </read_first>
  <behavior>
    - Test 1 (E2E): POST /api/sessions/test-session/intake/messages com body { chatId, text } retorna 201 e um lead com id e intake_status='in_progress'
    - Test 2 (E2E): GET /api/sessions/test-session/intake/leads/{chatId} apos o POST retorna 200 com o mesmo lead (case_data.messages contem o texto enviado)
    - Test 3 (E2E): POST duas vezes com o mesmo chatId resulta em UM unico lead (verificado via repositorio: count === 1)
  </behavior>
  <action>
    Criar DTOs em `src/modules/intake/dto/index.ts`: `IngestIntakeMessageDto` com `@IsString() @MaxLength(100) chatId: string` e `@IsString() @MaxLength(4096) text: string` (usar class-validator, mesmo estilo de webhook.dto.ts). Exportar tambem uma interface/tipo de resposta simples.
    Criar `IntakeController` decorado `@ApiTags('intake')` e `@Controller('sessions/:sessionId/intake')`. Injetar `IntakeService`. Rotas:
    - `@Post('messages') @RequireRole(ApiKeyRole.OPERATOR)` -> `async ingest(@Param('sessionId') sessionId: string, @Body() dto: IngestIntakeMessageDto)` chamando `intakeService.ingestMessage({ sessionId, chatId: dto.chatId, text: dto.text })`, retornando o lead (201 default do @Post).
    - `@Get('leads/:chatId') @RequireRole(ApiKeyRole.OPERATOR)` -> `async read(@Param('chatId') chatId: string)` chamando `intakeService.getByChatId(chatId)`.
    Anotar com `@ApiOperation`/`@ApiResponse` no estilo do webhook.controller.ts.
    Criar teste `test/intake-tracer.e2e-spec.ts` seguindo test/integration-fabric.e2e-spec.ts: bootar AppModule via Test.createTestingModule, aplicar `applyGlobalValidation`, `app.setGlobalPrefix('api')`, obter `Repository<IntakeLead>` via `moduleFixture.get(getRepositoryToken(IntakeLead, 'data'))`, e usar supertest para exercer os 3 comportamentos acima. Rota completa e `/api/sessions/test-session/intake/messages` (setGlobalPrefix('api') + @Controller). O teste deve autenticar como qualquer rota E2E autenticada existente (reusar o setup de header de API key do harness de test/integration-fabric.e2e-spec.ts / test/setup-e2e.ts).
  </action>
  <verify>
    <automated>npm run test:e2e -- intake-tracer</automated>
  </verify>
  <done>test/intake-tracer.e2e-spec.ts passa: POST /api/sessions/test-session/intake/messages cria lead (201), GET retorna o lead persistido, e dois POSTs com o mesmo chatId produzem exatamente 1 lead. O caminho HTTP -> service -> entidade -> DB -> leitura esta provado end-to-end e commitado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP -> IntakeController | Entrada nao confiavel (chatId, text) cruza aqui; validada por class-validator DTO |
| n8n/webhook -> IntakeController | O workflow n8n (Plan 03) chamara estas rotas com API key OPERATOR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | POST /intake/messages body | medium | mitigate | IngestIntakeMessageDto com @IsString/@MaxLength; applyGlobalValidation com whitelist |
| T-01-02 | Spoofing | rotas intake | high | mitigate | @RequireRole(ApiKeyRole.OPERATOR) — sem @Public; guard de API key existente aplica |
| T-01-03 | Denial of Service | case_data.messages array cresce sem limite | low | accept | Tracer usa upsert simples; limite de tamanho tratado na expansao (Plan 02) |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo para arquivos do modulo intake
- `npm run test:e2e -- intake-tracer` passa (3 comportamentos)
- Glob da entidade presente em app.module.ts E data-source.ts
- IntakeModule presente em app.module.ts imports[]
</verification>

<success_criteria>
Uma mensagem entra via HTTP, um lead e persistido na conexao 'data' (SQLite no teste), o lead e legivel por chat_id, e reenvio nao duplica — tudo coberto por teste E2E verde.
</success_criteria>

<output>
Create `.planning/phases/01-bot-de-intake-e2e/01-01-SUMMARY.md` when done
</output>
